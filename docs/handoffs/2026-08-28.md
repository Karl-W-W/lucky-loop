# Handoff — 2026-08-28

Written for the next session. **Every claim here is a hypothesis.** Five
inherited claims died on contact today; the last one was the clock.

REDACTION: this file is in a PUBLIC repo. No hostnames, addresses, tunnel
names, key fingerprints or auth URLs appear below, by rule — not by accident.

## SHIPPED (with numbers)

- **R-5 closed.** The tunnel token is off the process command line: argv match
  count **0**, token at rest `600 root:root` (184 bytes) instead of inside a
  world-readable `644` unit. Service `active`, dashboard-managed ingress
  **preserved** (config `version=8`), round-trip **1.12 s**. Executed by Karl at
  root, one command at a time, token never printed.
- **A second route to the DGX proved for the first time.** Key-based login in
  **1.36 s**. The peer's first non-zero handshake ever recorded; bytes moved
  (tx 5044 / rx 5260) where the counters had read 0/0 since the peer was added.
- **Mesh SSH disabled on the host** so port 22 is served by real sshd with a
  pinned host key, removing a browser + third-party control plane from the
  break-glass path. Host key verified against an independent path before pinning.
- **MCP allowlist is now an enforced contract** (`data/mcp-allowlist.json` +
  `scripts/check-mcp-invariant.mjs`, in `prebuild`). 76 tools, **0**
  write-capable, both hosts identical. Both red states demonstrated before it
  shipped. Deployed to production and verified READY.
- **Inspector grades agents instead of the harness.** Real failures **23 -> 10**.
  Fixture runs excluded (tagged, not deleted; `--include-fixtures` restores
  them: 2 runs/0 failures vs 14/12). Uninstrumented is no longer scored as
  failed. A consent refusal is no longer graded "no metacognition".
- **Inspector reads real events at all** — the live path had never once run: the
  mapper was imported from a module that reaches React, and after the repo move
  it failed unconditionally, so even the mock report could not regenerate.
  Mapper extracted to a dependency-free module; mock report regenerated
  byte-identically, proving the refactor changed no behaviour.
- **Librarian's first applied proposal.** Golden set **16 -> 20**, rule 5
  carve-out applied, eval on 20 cases: pile_accuracy **1.0**, act_recall
  **1.0**, draft_coverage **1.0**, send_violations **0** — SHIP PASS.
- **Retrieval baseline recorded**: 61 pairs, hit@1 **18/61**, hit@3 **32/61**,
  MRR@5 **0.422**, 22 misses, query cache purged 64 -> 0 first. The 0.422 was
  real but had never been written to the dashboard until now.
- **Investigated and deliberately NOT changed**: the perception agent's run
  lifecycle. Both real runs carry start..completed and the orchestrator emits
  completion on success and error paths. The apparent gap was 100% fixtures. No
  edit was made to live trading code for a non-bug.

## PRINCIPLES RATIFIED

1. **No indicator ships unless its failing state is reachable and has been
   demonstrated.** Someone must have watched it go red on purpose. Pair every
   probe with a reading that moves — a byte count, a timestamp, a completed
   session. Liveness of the checker is not liveness of the thing checked.
2. **Two populations, one floor.** The event-emitting trading agents are
   OBSERVED but not GOVERNED. The Librarian writes instructions only for roster
   agents that have a yaml. **No yaml without a roster entry.**
3. **Rule 5 automation carve-out** (mail triage): a no-reply robot cannot
   receive a draft, so act-pile noise is not visibility. This makes rule 6c
   reachable, which it never was for a listed domain.
4. Derived, never typed. A hardcoded "0 write-capable" in a new gate printed a
   clean line over a contract containing a write tool. Caught by its own red test.

## OPEN DECISIONS

- **Domain** — Karl names one in the morning. Checklist below, unchanged.
- **One commit awaits push authorisation** (see FINAL STATE). Authorisation
  named two SHAs; a third was created afterwards by approved work, so it was
  not pushed.

### Make-/war-public checklist (carried unchanged)

Confirm `REPO_PUBLIC` and real repo visibility still move together; the six
Assets rows all resolve (a broken link is now a visible 404, not a dropped row);
the ledger stamp reads honestly on production; any new route with its own
`openGraph` also re-exports its image; **web analytics is still off, so there is
no traffic baseline for any launch claim, before or after.** No additions are
required to ship. No redactions are required on the page: it renders only
committed JSON that has passed the gates. The exposure is the repository's
history, not the page, and pointing a domain at it changes nothing about that.

## IN FLIGHT

**The reranker, on the DGX, detached and surviving a sleeping laptop.**

- `rerank-provision.sh` — **pid 2400044**, PPID 1, own session, no tty.
  Builds llama-server (done, **CPU-only: there is no CUDA compiler on the box**)
  and downloads a 4B reranker GGUF (~2.48 GB), then writes a sha256 pin.
- `rerank-chain.sh` — **pid 2415036**, PPID 1, own session, no tty.
  **What is chained to fire:** waits for the provisioner's DONE marker, prints
  the hash pin, starts the rootless user unit, and records whether the model
  actually LOADS. It deliberately does **not** touch retrieval config — that
  changes behaviour for every agent and is a morning decision with a human.
- Logs: `~/rerank-provision.log`, `~/rerank-chain.log`. Unit staged and rootless
  (user unit, home-dir binary and model, loopback only).

**THE ONE MORNING CHECK COMMAND:**

```
ssh dgx-remote 'tail -20 ~/rerank-chain.log'
```

Expect `CHAIN_RESULT: MODEL_LOADED_OK`. If it says `MODEL_DID_NOT_LOAD` or
`PROVISION_INCOMPLETE`, nothing is broken and nothing was configured — the
reranker is simply not available, and the retrieval baseline stands untouched.

**No eval verdict exists yet.** Do not report one. Comparing against 0.422
requires purging the query cache and taking BOTH readings, and the reranker must
be pointed at by retrieval config first.

## DATED PREDICTIONS

- **~2026-09-03**: the failure tile's guard trips. 30 of 34 reaps came from one
  wedged session, all dated 2026-08-27; when they age out of the 7-day window
  the count falls, and the sync refuses to write a smaller number. The tile then
  freezes at the high value while the host is healthy. Falsifiable: if the tile
  updates downward that week, this prediction was wrong.
- **The wedged session is not yet proven fixed.** Zero reaps since the service
  restart, but the stream had already been quiet for 5.5 hours beforehand, so
  the quiet is not attributable. Needs a day.

## SESSION 4 SHORTLIST (as proposed)

Hard constraint: at least half must produce something an outsider can see, or
move a D grade. Audit-only items rejected by default.

1. **Domain + public /war** — the only item that moves Product.
2. **Dead origin behind two published hostnames** — they serve errors to
   strangers *today*, which matters the moment a domain is handed out.
3. **The honest GPU question** — what workload actually exercises that box.
   **The reranker does not answer this**: it built CPU-only, so it must not be
   counted against the DGX grade even if the eval improves.
4. Tile guard asymmetry (support; the prediction above lands mid-session).
5. Userspace mesh networking (support) — until then the second route is
   ceremonial: it works only while the commercial VPN is disconnected.

Deferred: the host firewall contradiction (needs root, bundle with other root work).

## CORPSES — claims that died on contact today

1. **"There is a second route to the DGX."** There was not. The routine check
   was a control-plane probe that returns success independently of whether data
   can cross; it returned identical success in the broken and working states,
   while the handshake timestamp read epoch-zero. The route had never worked —
   not worked-then-broken. Fixed and proven the same day.
2. **"The rotation runbook tells you how to do R-5."** Its step-1 command could
   not run here (missing prerequisite) and, if forced, would have switched the
   tunnel to a mode with no routing config — leaving the process reporting
   healthy while nothing routed. Caught at preflight; runbook corrected in place
   with the original kept, labelled do-not-run.
3. **"Draft the gatekeeper yaml from its 31 real trajectories."** That agent has
   exactly ONE event in the entire table, no roster entry, and is not a factory
   agent at all. The "31" was every run across all agents on two days. Refused;
   the refusal produced principle 2.
4. **"The Librarian is blocked for want of an agent yaml."** My own error, from
   checking one agent instead of the set. A roster agent with a yaml and six
   real failures was available the whole time — and it became the first applied
   proposal the same evening.
5. **"There was an overnight."** There was not. Both machines read
   2026-08-28 22:40 when the next session opened; roughly ten minutes had passed
   since the previous write. I also misread a healthy six-minute-old download as
   hours-dead before checking the clock, and corrected it in the same breath.
   Nothing ran overnight because no night occurred.
