# Handoff — 2026-09-15 (commander, clock-out 19:05 CEST, a short session before a Mac restart)

**Exit code: 1.** Nothing broke on the box or on the site, but the vault's canonical `master` has been
frozen since 2026-09-11 05:00Z while the box kept committing on a foreman branch. Everything is safe and
on the bare now; getting it into `master` is one card for Karl's hand. Five days of predictions from the
09-10 handoff held; one of my own corrections was wrong a second time (CORPSES).

REDACTION: PUBLIC repo. No hostnames, tunnel names, topic names, key paths, account ids, fingerprints.
"The loop host" is the box; "the mirror" is the private artifact repository; "the bare" is the vault's
canonical repository on the loop host.

## SHIPPED (with numbers)

- **The hop ran unattended for five days.** 16 scheduled Action runs since 2026-09-10 20:59Z, every one
  green (GitHub's cron fires up to 2.5 h late; that is the schedule, not a fault). The loop host staged a
  status snapshot every 12 h (`gate=strong tokens=6 pushed=yes`) and skipped the half-hours between;
  one transient `mirror-unreachable` on 09-11 17:35Z, recorded in the failures log by design. PR #1 was
  refreshed twice a day and is **still open** (head f6b1959).
- **ntfy-actions was built by the foreman on 2026-09-11**, claimed 05:00Z as predicted, verified, and its
  listener is live: KR-0 reads **10/10** with `decide-listener` beating every 60 s. It cannot push to the
  vault's `master` (below) and it cannot push to the phone until Karl pastes the alerts topic (a card).
- **Today, this session**: the branch stranding found and secured (31 commits pushed to the bare, tree
  clean); the merge card extended with the mirror line and the finding; a board task for the foreman
  defect (`host: mac`); session events on the same branch; the gates-on-PR wording corrected a second
  time in four places (53b058e, canvas regenerated); memory updated with the clock-in check.
- **Graded, the 09-10 predictions**: scheduled run green ✓ (20:59Z); bills-to-loop 0 new every night ✓
  (5 passes on the host, newest 09-09); foreman claims ntfy-actions on its first tick after `not_before`
  ✓ (05:00:46Z); scanner UNHEALTHY at ~01:00Z every night ✓ (09-11 to 09-15, `9/10 fresh`). O3/KR4
  reads `1/7 · 6 of 7 nights idle (skipped)`, exactly the definition.

## PRINCIPLES

1. **A refused push is a stranding, not a no-op.** The gate did its job; nothing returned the clone to
   `master`, and four days of records took the branch. A clock-in that checks `branch --show-current`
   on the box would have seen it on day one. It is in THE ONE MORNING CHECK COMMAND now.
2. **Do not write a brief that asks for a protected path.** The dissent required tests; the vault gate
   protects `tests/`; my brief did not say where the test may live. The worker did what it was told.
3. **The run list decides, three times if needed.** "gates.yml does not run on the bot's PR" → "it runs"
   → "it queues and waits for approval (`action_required`)". Only the third survives five days of runs.
4. **Do not move `master` while a fast-forward is waiting.** Every write of this clock-out went onto the
   box's branch on purpose, so Karl's merge stays one command and the append-only log never rebases.

## OPEN DECISIONS (in `queue/needs-you.json` — on the box's branch until the merge; the bare and the Mac still show four)

1. **Merge `foreman/ntfy-actions-remote-words` into `master` with the human override** — four exact
   lines on the card (checkout, merge, `push -o gate=karl`, then the Mac pull and mirror push). Until
   then nobody pushes to `master`.
2. **Paste the alerts topic** into the box's ntfy env file (yours, 1 min) — then cards reach the phone.
3. **Merge PR #1** (status snapshot; the Action keeps it fresh) — one tap.
4. **The rule**: a classifier refusal is a stop, a card at its own tier, never another route — adopt / amend / reject.
5. **ntfy app on the phone** (2 min).
6. **R-1** notes vault, move + scrub — its card expires today; extend it or do it.

## IN FLIGHT

- **The Action**, every ~6 h, green, refreshing PR #1 until it is merged; after that, "nothing to
  publish" until a new pass or 12 h. Check: `gh run list -R Karl-W-W/lucky-loop --workflow artifact-return.yml --limit 2`.
- **The loop host**: artifact-return timer at :05/:35; the loop timer every 10 min, idle (no bill in five
  nights); the nightly at 01:32Z; the foreman skipping both open tasks (`host: mac`); the listener polling.
- **Two open board tasks for a Mac session**: `foreman-return-vault-clone-to-master` (do it AFTER the
  merge card, never before) and `polysignal-scanner-night-heartbeat`.
- **This Mac**: 14 idle herdr agents plus this one; Karl terminates the Terminal tab for a restart, which
  is safe (transcripts on disk, herdr resumes the panes; no repo on this Mac has unpushed work).
- **Not done here**: the vault copy of this handoff — it would be a commit, and commits wait for the merge.

**THE ONE MORNING CHECK COMMAND**

```
dgx-today --brief && ~/brain/tools/heartbeat check && ssh dgx-remote 'git -C ~/brain branch --show-current; git -C ~/brain rev-list --count origin/master..HEAD'
```

Expect `NEEDS YOU: 6` or fewer, `BOX: OK`, `KR-0 PASS 10/10`, and the last two lines `master` / `0`.
`foreman/ntfy-actions-remote-words` / `31` or more means the merge card is still waiting.

## DATED PREDICTIONS

- **Until the merge card runs**: the bare's `master` stays at 2026-09-11 05:00Z, the Mac and the GitHub
  mirror show four open cards while the Today page shows six; the count on the box grows by one nightly
  commit a day. Falsified if any push reaches `master`.
- **2026-09-16 ~01:00Z**: the scanner UNHEALTHY transition again (task open, not built).
- **2026-09-16 ~01:45Z**: bills-to-loop 0 new; O3/KR4 still `1/7`, 7 idle nights.
- **Within 15 min of the merge**: the post-receive sync indexes the branch's pages; KR-0 stays 10/10;
  the Today page's count is unchanged (it read the branch all along).

## SHORTLIST

1. The merge card (four lines, Karl's hand), then the topic paste and the phone app: the tap path goes live.
2. Merge PR #1; say the rule's word.
3. A Mac session: the foreman stranding task, then the scanner heartbeat task.
4. Derive O3's key results at build time on /war (KR4 is derived on the Today page only).
5. Feed the loop: five idle nights is the starving state, not a failure.

## CORPSES

- "gates.yml runs on the Action's PR as well" (mine, 09-10 clock-out) — on a bot-pushed PR it only queues
  and waits for a maintainer's approval (`action_required`, every push 09-11 to 09-15). Corrected in the
  workflow body, AUTONOMY.md, CLAUDE.md and the canvas node.
- "Three vault copies agree" (mine, 09-10) — true that evening; from 09-11 05:00Z the box's clone was on
  a task branch and no clock-in ran to see it. Root cause on the board; the check is in the morning command.
- My ntfy-actions brief demanded a tests file and said nothing about the vault gate's protected `tests/`
  path; the refusal was the gate working and my brief failing.
- "PR #1 merged" (Karl, 09-10) — still open on 09-15, refreshed by the Action twice a day; a card.
- The first version of today's box script never ran: a quoting error in the shell wrapper. Rewritten as
  files copied to the box; nothing on the box changed until the rewrite ran.
