# Handoff — 2026-09-04 — the Loop status panel on /war

REDACTION: public repo. No hostnames, tunnel names, tokens or paths of private machines.

## The finding: the shortlist item was a corpse

The dispatch said "Loop status panel on /war, half (b) of move 1, NOT built". It was
built on **2026-08-21 at 14:56** (`d442675`, `app/war/components/LoopStatusPanel.tsx`),
fifty minutes after the vault line that still called it missing was written. That line
survived two later edits of the entity page and was handed to this session as work.
**Nothing was rebuilt.** The vault entity is corrected (pushed to the bare, host tree
fast-forwarded to the same commit), so the same dispatch cannot recur from that page.

## What the existing panel already does (verified by reading, then by rendering)

- Six states in `dueState()`'s shape: `unknown | stale | down | failing | starving | working`.
  Stale is checked BEFORE health, at 24 h, so no present-tense claim rides on old data.
- Zero cases: no file → "Unknown"; no pass → "never"; queue `null` → "not reported";
  no doc types → "none recorded". `syncedAt` is in the header and footer, beside every number.
- Real data only, from `data/loop-status.json`; static import, no fetch, zero API routes.

## Shipped and verified

`data/loop-status.json` refreshed by `npm run sync:loop` (counts and timestamps only;
runs and loop-def came back byte-identical). Prod's panel had read **Snapshot stale**
for nine days. It now reads **Idle — nothing queued**: three passes, empty queue, last
tick exit 4 (idle), last failure still 2026-08-11. The loop is alive and unfed.
`data/ledger.json` was regenerated by the prebuild (picks up the previous commit).

`npm run build` green: ledger (77 commits), redaction gate with the local deny-list
loaded (5 artifacts clean), drift gate (40 nodes), agents registry, MCP allowlist (76
tools, 0 write-capable, both hosts live). `python3 loop/test_redaction.py` PASSED.
Production build served locally: the panel renders the idle state with the new age.

## Not done, on purpose

- No change to the panel's code, tokens or layout: nothing was wrong with it.
- No TypeScript test runner for `loopState()`; the repo has none. States verified by reading.
- The queue is still empty. Breadth (O3) moves only when a human feeds it.
