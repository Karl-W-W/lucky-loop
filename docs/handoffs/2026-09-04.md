# Handoff — 2026-09-04

Written for the next session. **Every claim here is a hypothesis.**

REDACTION: this file is in a PUBLIC repo. No hostnames, tunnel names, key fingerprints,
token values, auth URLs or personal names appear below, by rule.

## SHIPPED (with numbers)

- **The needs-you queue went from 7 open items to 1.** Closed today, each with its check
  passing in Karl's own terminal: the two credential rotations (old values answer 401 /
  unauthorized), the DGX platform upgrade (chosen A, executed, verified: containers 3/3
  back, vault 171 pages, checks 13/13), the SSH key replaced by a passphrase-protected one
  (old key removed from the box and deleted locally), 8 retired repos archived + 5
  descriptions set, iCloud mail credential in place (IMAP login proven: 57 unread of
  45,307). Remaining: the Apple Notes → password manager migration, a hand job.
- **herdr adopted as the agent workbench.** Config with explicit bindings, toasts, sound,
  resume; six workspaces (tour, lucky-loop, polysignal, brain, platform, dgx); Claude and
  Hermes state integrations; a blocked agent becomes a queue item automatically (fired live
  twice on the first-run trust dialog, closed itself when answered).
- **Voice.** Whisper in (local, on the loop host), ElevenLabs out (voice "George") for
  the Desktop, the herdr shortcut, and tonight's first morning-brief mp3. Free tier:
  10,000 chars/month; a brief is ~800.
- **Five task agents, all completed and reviewed:** the /war Loop panel already existed
  since 08-21 (snapshot refreshed, CI green, prod verified); PolySignal accuracy truth
  written to the vault (dir. acc 57/54/56 %, friction win 29/33/36 %, edge vs price
  negative, gate stays CLOSED); vault orphans 92 → 12 (38 pages linked, 3 commits); nightly
  queue audited (5 nights = 1 fresh fact + 1 real finding) and a `morning-brief` job added
  and run (4 s); iCloud IMAP tool built (stdlib, no SMTP anywhere) and both mail jobs
  rewired to it with the router model, tested against a stubbed mailbox and the real server.
- **Tailnet-first ssh.** The DGX alias goes direct over Tailscale when it answers and
  falls back to the tunnel by itself. NordVPN dropped by Karl; a guard notifies on loss.
- **Today page**: one-line hint to `needs-you` (menu: run item N line by line, d<n> closes).

## PRINCIPLES

1. **Secrets never cross the screen.** Every rotation today ran through a hidden-input
   helper that proves the new value works before writing and proves the old one is dead
   after (R-2: 200 then 401). Incident: the first iCloud helper asked two questions and
   Karl typed his account password into the first; the file was deleted and the helper
   rewritten to accept only the app-specific format.
2. **The popup is not a colleague.** Karl asked "which key?" inside a script and nobody
   answered. Anything that needs a reply belongs in the chat, one step per turn.
3. **Check git history before building what a list calls missing.** The Loop panel had
   shipped 50 minutes after the note that called it unbuilt.
4. **A reboot is a test of enablement.** One enabled service (the market scanner) did not
   return after the platform upgrade and had to be started by hand.

## OPEN DECISIONS

1. R-1 Apple Notes → password manager (Apple Passwords recommended; built in, iCloud,
   on the phone already). Eight steps, one per sitting; seed phrase to paper first;
   step 8 (empty Recently Deleted on both devices) is the one people skip.
2. ElevenLabs plan: free tier runs out after ~12 briefs; Starter covers a month.
3. The truth agent's accuracy dashboard reached vault master directly although
   `dashboards/` is a gate-protected path meant to arrive via a proposal branch Karl
   merges. Verify how the gate let it through; if an override was used, ratify or revert.
4. The PolySignal lever branch (below) — merge or not, after reading the back-test.

## IN FLIGHT

- **nightly** (herdr agent, dgx workspace): fixing the two stale nightly jobs so each
  night starts a fresh model session; proving with a W36 report; re-running
  morning-brief now that the voice key exists. Logs in the agent pane and the vault.
- **truth** (herdr agent, polysignal workspace): implementing "no Bullish calls below
  0.5" on a branch with a side-by-side back-test; pushes the BRANCH only, opens no PR.
- 03:30 nightly-queue.timer: morning-brief first, then the mail jobs (first real night on
  iCloud: 4 bill candidates, 37 unread in the last 3 days), then the rest.

**THE ONE MORNING CHECK COMMAND** (on the Mac):

```
dgx-today --brief
```

Expect `NEEDS YOU: 1 item(s)`, `AGENTS: … 0 failed` with `bills-to-vault` and
`inbox-drafts` no longer marked blocked, `BOX: OK`. A `blocked (credential)` on the mail
jobs means the IMAP login failed overnight; `failed` on morning-brief means the router or
the voice API was down; `NEEDS YOU … absent` means the vault tree on the box did not pull.
Also: `ls ~/brain/captures/morning/` should show today's `.md` and `.mp3`.

## DATED PREDICTIONS

- **2026-09-05 03:35**: `captures/bills/2026-09-05-bills.md` exists with 1–4 lines and at
  least one PDF under the admin-billing inbox realm. Falsified if the capture is missing
  or the jobs report blocked.
- **2026-09-05 morning**: the mp3 beside the morning page plays in George's voice.
  Falsified if only the `.md` exists (voice API or key path broken).
- **2026-09-06**: with R-2 and R-3 closed, /war shows O2 met rather than overdue.
  Falsified if the derived KR still reads the queue as open.

## SHORTLIST

Constraint: at least half must produce something an outsider can see, or move a D grade.

1. Read the lever back-test; merge or reject the branch (moves the accuracy number
   outsiders see on /war).
2. Feed the loop 3–5 real non-invoice documents now that bills arrive by themselves
   (O3, outsider-visible on /loop).
3. R-1 step 2 (seed phrase to paper), then one step per sitting.
4. Entity pages for the three dangling links the gardener found.
5. Decide the ElevenLabs plan before the free tier runs dry.

## CORPSES — claims that died on contact today

- "The Loop status panel is NOT built" (shortlist since 08-21) — it shipped 50 minutes
  after that note; caught by the panel agent reading git history first.
- "The nightly queue delegates to the local router" (my own briefing) — it calls the Max
  plan; the nightly agent corrected it and used the router only for the new job.
- "The vault database is called brain" (the upgrade runbook) — it is `gbrain`; the restore
  proof failed once on the wrong name before passing (171 = 171).
- "NordVPN's kill switch is what strands Tailscale" — the kill switch was already off;
  the app blocks the control plane regardless. Only dropping it, or Mullvad via Tailscale,
  works.
- "The 111 orphan pages" — 92 by the link definition used; 111 came from a different
  measurement; the index counted 128 because it does not resolve event links.
- "Prediction (a) held: the nightly page names the checkout commit" — the commit appears
  only in the scout's prose and the journal, not as a queue-written line.
- "Hermes cannot be started headless as a herdr agent" (implied by earlier sessions) — it
  can; only its own hook-approval prompt is invisible to herdr's blocked detection.
