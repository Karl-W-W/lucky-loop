# Handoff — 2026-09-03

Written for the next session. **Every claim here is a hypothesis.** This file also
closes 2026-09-02, which ended without a clockout: no handoff was written that day,
and a memory note pointed at one that never existed.

REDACTION: this file is in a PUBLIC repo. No hostnames, tunnel names, key
fingerprints, token values or auth URLs appear below, by rule.

## TOMORROW, IN ORDER (Karl departs Fri 2026-09-05, returns Thu 2026-09-10)

1. **Open the Today page** (Hermes Desktop, sidebar row "Today"). It is the morning
   check now: NEEDS YOU first, then what the agents did, then goals, then the box.
   Expected on a quiet morning: "6 things need you." (or fewer, as you close them),
   agents 0 failed, box OK, checks 13/13 and fresh. Anything else deserves a look.
2. **Close what only your hand can close**, from the top of NEEDS YOU: the two
   rotation items (R-2, R-3), the reboot decision (recommendation unchanged: B, after
   the 10th), the GitHub archive/descriptions block (copy-paste, below). Closing an
   item means editing `queue/needs-you.json` in the vault: `done: true` plus
   `doneOn`. Agents add items; they never close them.
3. **Away mode stays as designed on 08-31**: nothing new runs unattended. The
   nightly queue gained one step (a fast-forward pull of the checkout it reads) and
   nothing else.

## SHIPPED (with numbers)

- **The OKR board has live objectives again.** `data/okrs.json` held one objective,
  met on 2026-07-30, for 35 days. It now holds **5 objectives, 13 key results**; O1
  stays met, O2 (exposures, due 09-05), O3 (breadth, 10-31), O4 (one screen, 10-15),
  O5 (the vault, 10-31). Every KR names the file or event stream it is measured
  from. Rendered on prod within one build; canvas regenerated; drift gate green.
- **The Today page exists, beside Fleet.** The Hermes Desktop plugin now registers two
  rows: Today (four blocks in reading order: needs you · what the agents did · goals ·
  the box) and Fleet (the full box view, unchanged). Frontend **624 lines**, hot-reloaded from the
  runtime door; backend **716 lines** mounted under the Fleet router as `/today` and
  `/today.txt`. **13 of 13 headless checks pass**, including both failing states
  (first-sample failure, refresh failure after a good sample) and recovery. The text
  endpoint is the same page as a digest an agent can read in one call.
- **Derived, not typed.** Four key results compute themselves: O2/KR1 from the queue's
  done flags, O3/KR2 from the loop's own record (3/5 passes, 3/3 doc types = 80 %),
  O4/KR3 from the queue's since→doneOn latency (nothing closed yet = 0 %, oldest open
  item 42 days), O5/KR2 from the retrieval-eval dashboard's last row (0.422). The rest
  render with the word "declared" beside them.
- **The needs-you queue exists in the vault** (`queue/needs-you.json`, 6 items) with the
  rule ratified as O4/KR2: an item exists there before it is mentioned in chat. Plus
  the vault's **first person page**, so every agent reads the same Karl.
- **The nightly scout was reading a checkout 15 commits behind main for 9 days.** Its
  product report said "Unchanged — same three checksums" every night because nothing
  refreshed the checkout. Fixed twice: the checkout was fast-forwarded today, and the
  nightly queue now pulls (ff-only) before it delegates. Backup of the script kept
  beside it. Dry-run clean.
- **The 09-02 leftover is pushed**: the one-line pre-commit hook fix. CI green in 36 s.
- **Four idle terminal windows closed**; one session remained, this one.

## PRINCIPLES

1. **A key result that flatters itself is worse than a declared number.** The first
   latency derivation read decision→outcome pairs from the ledger and reported a
   0.1-day median at 100 %, because a clockout writes both events in one sitting. It
   measured nothing. Measure where the hand is actually recorded (the queue).
2. **A checkout an agent reads from is part of the data path.** If nothing refreshes
   it, every report from it is stale and says so to nobody (15 commits, 9 days).
3. **An item exists in the queue before it is mentioned in chat.** Otherwise "for your
   hand" items live in transcripts and are re-derived every session.
4. **Outward account changes are for Karl's hand, by the permission gate as much as by
   rule.** When refused, hand them over as one copy-paste block; do not retry.
5. **Push the vault to the bare, not to the branch's upstream.** The Mac clone tracks
   the GitHub mirror; a push to the bare was rejected non-fast-forward until rebased.
   The two-writer note predicted exactly this.

## OPEN DECISIONS (all six are in `queue/needs-you.json`, in this order)

1. R-2: revoke the leaked API key. Console balance is 0 today, so it becomes spend the
   day the console is funded — **do not fund the console before this**.
2. R-3: rotate the Telegram bot token (step 2a alone stops the bleed).
3. The DGX reboot: A (before travel) or B (after 09-10). Recommendation: B.
4. R-1: the Apple Notes credential vault → password manager, one step per sitting.
5. The DGX SSH key passphrase (runbook §5), at home, with a second terminal open.
6. GitHub: archive 8 retired repos and set 5 descriptions. An agent was refused this
   by the permission gate today. Copy-paste:

   ```
   for r in AI-Trading-Bot pool-protocol git-github.com-berger-studios-poolmusic-artist-admin \
            https-github.com-berger-studios-poolmusic-mobile poolmusic-artist-admin-new \
            polysignal-pitch polysignal-ops-demo luckyloop-site-archive; do
     gh repo archive Karl-W-W/$r -y
   done
   gh repo edit Karl-W-W/polysignal-engine --description "PolySignal — prediction-market signal engine (LangGraph pipeline). Paper trading only; the live-trading gate stays closed until accuracy thresholds are met."
   gh repo edit Karl-W-W/lawfirm-pipeline  --description "PRIVATE: law-firm client-acquisition pipeline. Every draft queues for human review; nothing is sent automatically."
   gh repo edit Karl-W-W/polysignal-os     --description "PRIVATE: PolySignal deploy scripts, ops notes and archive. The runtime lives in polysignal-engine."
   gh repo edit Karl-W-W/platform          --description "PRIVATE: platform-selection meta-repo — Perception → Decision → Action workflow and ADRs for the agent stack."
   gh repo edit Karl-W-W/jarvis-os         --description "PRIVATE: earlier agent-OS work and empire-era planning docs (docs/empire-era). Kept for history; not the active line of work."
   ```

Not queued, noted: flipping polysignal-engine private would break the DGX's fetch of
it (anonymous https remote, no credential on the box). Revocation is the fix for R-2;
the flip only reduces future exposure and needs a credential first.

## IN FLIGHT

Nothing started by this session is still running. The Hermes backend on the DGX was
restarted three times to load the new routes (last at 17:16 local); the Desktop
reconnects on its own. The nightly queue runs at 03:30 as before, with one new step.

**THE ONE MORNING CHECK COMMAND** (the Today page as text; the header takes the pinned
Desktop session token, which lives in the DGX's Hermes env file and never in a repo):

```
ssh dgx-remote 'curl -s -H "X-Hermes-Session-Token: <token>" http://127.0.0.1:9119/api/plugins/fleet/today.txt'
```

Expect the first line `TODAY · <timestamp> · <host>`, then `NEEDS YOU: 6 item(s)` (or
fewer), `AGENTS: … 0 failed`, five objective lines, `BOX: OK`. A 401 means the token
changed; a connection error means the backend is down (`systemctl --user status
hermes-serve` on the box); `NEEDS YOU … absent` means the vault tree on the box has not
pulled.

## DATED PREDICTIONS

- **2026-09-04 03:30**: the nightly queue's page carries a line naming the checkout's
  commit, and the product report stops saying "Unchanged — same three checksums"
  (the checkout moved 17 commits today). Falsifiable: if tomorrow's report repeats the
  old checksums, the pull step did not run.
- **2026-09-06**: if R-2 and R-3 are still open, `/war` shows O2 as overdue. That is
  the board working, not a defect. Falsifiable by closing the items.
- Carried forward from 08-31: the failure tile's guard trips on the next manual
  `sync:failures` after ~09-04. Not touched today.

## SHORTLIST (next session; large items dated after 2026-09-10)

Constraint: at least half must produce something an outsider can see, or move a D grade.

1. Karl closes queue items → O2 moves on `/war` (outsider-visible; a 15-minute hand job).
2. Reranker GPU promotion + the 61-pair eval → O5/KR2 (moves a measured number).
3. Real documents into the loop → O3 (the only breadth that counts; a human chooses).
4. A `queue close <id>` one-liner so closing an item is one command, not a JSON edit.
5. Vault graph: link the 111 orphans from the pages that cite them; refresh the
   lucky-loop entity state block at clockout → O5/KR1.

## CORPSES — claims that died on contact today

1. **"The away-check command is in `docs/handoffs/2026-09-02.md`."** No such file was
   ever written; the 09-02 session ended without a clockout. Memory corrected.
2. **"The three 09-02 patches are staged, awaiting a yes."** All three were applied
   that afternoon (banner, nightly commit-on-halt, vault recovery).
3. **"The nightly scout reads current product data."** Its checkout was 15 commits and
   9 days behind main; "Unchanged" was true of the checkout, not the product.
4. **"Idea-to-action latency can be read off the ledger."** 0.1 days at 100 %, because
   decision and outcome are written together at clockout. Replaced by the queue.
5. **"`git log --format=%h|%cI` works inside a shell string."** The pipe was a pipe;
   the head came back empty and the goals block showed no commit.
6. **"The Mac vault clone pushes to the DGX bare."** Its upstream is the GitHub
   mirror. The first push to the bare was rejected non-fast-forward.
7. **"Archiving repos is a one-line agent job."** The permission gate refuses outward
   account changes; it is a one-line Karl job.
8. **"Renaming the Fleet row to Today keeps Fleet available."** From Karl's seat it was a
   removal ("why did you remove fleet?"). Fleet is back as its own row and page; Today
   sits above it. Add beside, never rename or hide a surface he uses.
9. **"A command with a trailing `# note` is a command."** Three pasted queue items failed:
   zsh handed the comment to `open`, executed a Telegram menu path as a filesystem path,
   and passed a comment to `ssh-keygen`. `interactivecomments` is off by default. The
   queue now keeps `steps`, `command` and `check` as separate fields.

## VAULT

`queue/needs-you.json` (6 items), `queue/README.md`, `entities/karl-wuerfel.md` — two
commits, pushed to the bare and the mirror, DGX tree fast-forwarded and verified by
the Today endpoint reading the queue. Session decision + outcome logged through
`brain-log.py`.
