# Handoff — 2026-09-08 (commander: run-until-finished v1.2 + Karl's ten-step order)

Written for the next session. **Every claim here is a hypothesis** — each one below was checked
live by the commander: the hook by piping Stop events and by three dry laps in a real worker
pane, the box by ssh, the repos by git.

REDACTION: this file is in a PUBLIC repo. No hostnames, tunnel names, key fingerprints, tokens or
auth URLs appear below, by rule.

Session shape: Karl pasted a v1.1 design from a chat; it described files that did not exist and a
repo it had not read. The commander verified the Claude Code hook docs, built v1.2, proved it
live, then Karl ruled (foreman owns it; KRs in the yaml; the worker never touches the verifier)
and issued a ten-step order with five standing rules. All ten steps ran; no paste was needed.

## SHIPPED (with numbers)

- **run-until-finished v1.2** in the factory repo: Stop hook bound to one session id, every
  invocation counted (cap 5), check script frozen by sha256 at arm time, timeout/crash never
  release; arm helper; SessionStart only on startup/clear. Unit-tested on 11 paths, then three
  dry laps in a herdr worker: CONTINUE→lap 2→lap 3→EXHAUSTED (3 hook lines), CONVERGED rejected
  with the failing KR quoted, CONVERGED released with the arm file gone. **Lap 1 for real:
  CONVERGED on the first stop**, 3 of 3 KRs pass, handoff committed.
- **KRs live in the yaml** (mail-triage): KR-0 heartbeat check, KR-1 golden set (20/20),
  KR-2 needs-you ≤ 6 via a box-side Today reader. The check script is a transcription with a
  drift guard; it exits 0/1/3 (pass/fail/cannot run).
- **Foreman on the box**: systemd user timer every 15 min, service runs the script from the
  vault path (a push deploys it), task type run-until-finished (one lap per tick, graded by the
  check script, queued behind ordinary tasks), heartbeat per cycle, R5 fields on the items it
  creates, refused tool calls logged from every transcript on the box into one file. First
  cycle claimed the feed-from-bills task within a second of enabling.
- **R4 heartbeats**: `~/brain/tools/heartbeat check` — 7 daemons registered, 7/7 fresh at
  clock-out, runs on the box, forwards from the Mac.
- **Read-only deploy key** for the factory repo on the box; clone in place with the push URL
  disabled (R2). **Watcher retired** (launchd job unloaded, plist renamed, decision logged).
  **hermes-scout** closed as a leftover. **Dead-man Worker** written, dry-run clean, private
  repo, checklist of the five things only Karl can create. **Librarian proposal** with three
  deviations filed; CLAUDE.md note added. **Runbook**: post-reboot acceptance test appended.
- Queue: decision `loop-factory-on-the-box` closed as deploy-key; `foreman-install` closed on
  Karl's order; every open item carries `default` + `expiry` (R5); 3 new hand items.

## PRINCIPLES

1. **The agent's word never releases a loop; a check does.** And nobody edits the check inside
   a loop: hash at arm time, deny rules, refusal on mismatch.
2. **Every verify line runs on the box (R3).** The Mac reaches the box through vault shims, so
   one yaml line is true on both hosts.
3. **Check lines run in plain form, never mutated.** A compound form (`…; echo $?`) fell outside
   the allow list and stalled a lap on a prompt.
4. **Deploy by push, not by copy.** The foreman service runs from the vault path, like
   infra-watch; the binary-copy install line is gone.
5. **A denial is logged from the transcript, not from a hook.** The PermissionRequest hook wrote
   nothing on the box's Claude Code version; the transcript records `This command requires
   approval` with `is_error`, and that is what the foreman sweeps.

## OPEN DECISIONS (all in the needs-you queue with default + expiry)

1. Dead-man switch: tunnel hostname, Access app + service token, Slack webhook, then deploy
   (`deadman-checklist-2026-09-08`). Five dashboard/secret steps; the Worker is ready.
2. A messaging platform for the Hermes loop profile, so lap recaps reach Karl's bot session
   (`hermes-platform-for-recap-2026-09-08`; factory card 013).
3. Factory settings: pin `Bash(~/brain/tools/:*)`, drop the Mac-only pin
   (`loop-factory-allow-pins-2026-09-08`; Librarian proposal b). Reversible; applies at expiry.
4. Unchanged: lever merge, gate install, loop-publish install, R-1 notes vault, vault mirror
   realign (the mirror is 3 behind / 64 ahead; a force-push is Karl's hand).

## IN FLIGHT

- Foreman cycle 1 on the box: worker on feed-from-bills since 16:39, verifier next; the board
  row and `captures/tasks/lucky-loop-feed-from-bills.md` show the verdict. Then the 15-min ticks
  take morning-brief-cap, then the mail-triage loop (row reopened, lap 0 of 3).
- The mail-triage pane worker (herdr workspace loop-factory) stays armed for its session; a
  prompt starts lap 2 in the pane. The box runs its own laps with fresh context.
- Nightly queue 03:30 as before.

**THE ONE MORNING CHECK COMMAND**

```
dgx-today --brief && ~/brain/tools/heartbeat check
```

Expect `NEEDS YOU: 9 item(s)` or fewer, `AGENTS: … 0 failed`, a `foreman` row in WORKLOG with a
path, `BOX: OK`, and `KR-0 PASS 7/7 daemons fresh`.

## DATED PREDICTIONS

- **2026-09-08 by 18:00**: the feed-from-bills row reads `verified` or `open` with a verifier's
  reason, and a foreman row with a path is in WORKLOG. Falsified if the row is still `claimed`.
- **2026-09-09 03:40**: the nightly queue ran and KR-0 still passes (nightly-queue heartbeat
  = today's report file). Falsified if `heartbeat check` lists nightly-queue as FAIL.
- **First foreman lap of mail-triage on the box** (within 2 ticks after morning-brief-cap): line
  1 of the box's handoff is CONVERGED and the row goes `verified` — because all three KRs pass
  today. Falsified if the lap ends BLOCKED on a KR that cannot run on the box.

## SHORTLIST

1. Karl's five dead-man steps (one afternoon; the Worker then alerts within 30 min of a silent box).
2. The Hermes platform for recaps (card 013); then `tools/loop-recap` (~15 lines, two call sites).
3. Apply proposal b (allow pins) so pane laps run without a single prompt.
4. Watch the first box lap of mail-triage; if it BLOCKs, the yaml line is wrong, not the loop.
5. Vault mirror realign (Karl's force-push); the box bare is canonical and 0/0.

## CORPSES — claims that died on contact today

- "The spec lives at templates/lucky-loop-run.md next to the protocol" — neither file existed
  anywhere; the chat had described files it never wrote.
- "checks.sh transcribes the yaml's verify lines" — the yaml had none; KRs were added by Karl's
  ruling before anything could be transcribed.
- "Write(…) deny rules protect the verifier" — Claude Code ignores them; only Edit(path) rules
  cover file tools. The Edit rules are in place; the inert lines stay as intent.
- "A PermissionRequest hook logs every denial on the box" — it logged nothing on the box's
  version in non-interactive mode; the transcript scan does the job.
- "`uname -a` being allowed with only Read means bypass mode" — no: read-only commands are
  auto-approved; a write via Bash and a network call were denied as designed.
- "The blocked-agent watcher would have queued the worker's prompt" — its log ends 09-05; it
  never fired today. Retired.
