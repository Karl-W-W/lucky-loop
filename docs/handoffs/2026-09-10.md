# Handoff — 2026-09-10 (clock-out of the 09-08 commander window, two days later)

Written for the next session. **Every claim here is a hypothesis** — each was checked against
git, the box, the live page or the herdr panes at clock-out.

REDACTION: PUBLIC repo. No hostnames, tunnel names, fingerprints, tokens, auth URLs.

The 09-08 handoffs (`2026-09-08.md`, `2026-09-08-evening.md`) carry the build. This file carries
what happened while nobody watched.

## SHIPPED (with numbers)

- **The loop earned its name once.** 2026-09-09 03:30 the nightly fed the two bills into the loop
  with no human; the loop passed them by 03:50; 05:47 the Mac publisher, from launchd, ran the
  gates and pushed **2 new passes** to this repo (commit 19dc339). No hand touched the chain.
- **Council 1 answered**: four fresh sessions on the box argued "move the artifact hop off the
  Mac"; the chair's word is **mac**, with a dissent worth reading (a closed lid for three weeks
  kills O3 silently, and nothing watches that hop). It waits as a decision item.
- **The box's first mail-triage lap converged unattended** (row verified, three KRs).
- **Dead-man**: 8 beats in the last 2 h, 0 alerts in 48 h; the 09-08 test alert and recovery
  aged out of the topic.
- **Heartbeats**: one false stale at 01:00 UTC today (the scanner rewrites its status file about
  every 30 min, not 15); cadence corrected in the registry, transition logged by infra-watch.
- Foreman since 09-09: the board drained; every tick "no eligible open task". Both vault remotes 0/0.

## PRINCIPLES

1. **Measure a cadence before you register it.** A guessed cadence is a false alarm with a date.
2. **A council's dissent is the finding.** The word was mac; the dissent names the unwatched hop.
3. **Unattended proof beats a demo.** One 03:30-to-05:47 chain is worth the day of setup.

## OPEN DECISIONS (all in the queue with tier, default, expiry ≤ 7 days)

1. Council: keep the hop on the Mac or move it — your word, action or mac.
2. PolySignal deploy handler hangs on an external socket in the full test run — marker, timeout
   or later.
3. ntfy app + subscribe (yours, 2 min). 4. Notes vault to a password manager (yours).

## IN FLIGHT

- Nothing on the board; the foreman ticks every 15 min and finds nothing. Add tasks with a brief
  and an acceptance, or council rows with options.
- Four herdr panes had re-opened the folder-trust dialog after a restart; answered at clock-out.
- A sibling session left a clock-in capture in the vault on 09-10; it is not in git.

**THE ONE MORNING CHECK COMMAND**

```
dgx-today --brief && ~/brain/tools/heartbeat check
```

Expect `NEEDS YOU: 4 item(s)` or fewer, `BOX: OK`, `KR-0 PASS 7/7`. Then `npm run sync:ops` here
and commit, so /war's three numbers move (needs-you, heartbeats fresh, laps converged unattended).

## DATED PREDICTIONS

- **2026-09-11 03:40**: the nightly reports "0 new" for bills-to-loop (no new bills) and the loop
  stays idle; falsified if it feeds a document nobody dropped.
- **2026-09-13**: the four tier-2 proposals in `queue/proposals.json` are applied or vetoed.
- **Within 15 min of any new board row**: the foreman claims it and a herdr tab appears on the box.

## SHORTLIST

1. Karl's two words (council, deploy handler) and the ntfy subscribe.
2. Watch the hop: a heartbeat for the Mac publisher (its log line) so a closed lid shows up.
3. Telegram token into Hermes, then the recap wire (card 013).
4. Feed the board: the next real tasks per project, each with an acceptance.

## CORPSES

- "KR-0 fails, a daemon is down" — the scanner was up; the registry's cadence was wrong.
- "The lever's deploy will say SUCCESS by itself" — the handler's full test run hangs on an
  external socket every time; the scanner was restarted by hand and the result says so.
- "sync:ops runs at clock-out" — it threw on KR-0's non-zero exit; fixed to read the JSON anyway.
