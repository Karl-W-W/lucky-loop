# Handoff — 2026-09-15 evening (commander; situational-awareness brief for Karl, drafted 20:05Z, facts re-run to 20:45Z, filed 21:05Z)

1. **Exit code proposal: 1.** Fleet green (heartbeats 10/10, failed units 0/0, Action 4/4), but the vault's `master` is still frozen at 09-11T05:00Z behind one card only Karl can run, and a retired publisher re-armed itself on the Mac tonight; the auto-mode classifier refused the commander's de-arm at 20:52Z, so by the rule card that is card `loop-publish-dearm-2026-09-15` — two lines, Karl's hand.
2. **Changed since the 09-15 clock-out (16:52Z):** PR #1 MERGED 19:52:03Z (`gh pr list`; the handoff said open); Hermes self-updated v0.21.1→v0.21.3 on both hosts 19:53–19:55Z; `89cae0d` "Today page: block 5 THE BOARD" pushed 20:10:04Z, gates green 20:10:08Z; the Mac rebooted 16:53:28Z → 5 herdr panes at the trust prompt, Tailscale stopped, `com.kww.loop-publish` reloaded; the Desktop fleet plugin threw a TypeError on 3/3 loads.
3. **Karl's order (1), DONE and VERIFIED:** truth (Mac) built the scanner sleep heartbeat under `queue/briefs/2026-09-15-scanner-night-heartbeat-tonight.md` — PR #4 on the scanner repo opened 20:10:47Z, CI green, merged `208d2d8` 20:12:55Z, box checkout on it, scanner restarted on the new code 20:14:47Z, cycle 1 complete 20:47:18Z with 0 errors. Board row `verified` (vault branch `c48dd8b`).
4. **Verified by:** truth-verify (Mac), not the builder — VERDICT VERIFIED at 20:48Z, every acceptance line re-run against GitHub and the box. The one proof left is the night: no `heartbeats -> UNHEALTHY` transition at ~01:00Z and a status-file mtime that advances every 25 min from 00:28Z.
5. **Deadline:** 2026-09-16T00:30Z — met at 20:14:47Z.
6. **Redaction:** PUBLIC repo — "the box" is the loop host, "the Mac" the interface; no hostnames, tunnel names, topic names, key paths, ids or fingerprints. All times Z.

## Q0 THE BOARD

### a) Hosts (asOf 20:12Z; `sw_vers`/`uname`/`uptime` on the Mac, `ssh dgx-remote 'cat /etc/os-release; uptime; free -g; df -h /; nvidia-smi'`)

| Host | OS / kernel | Up since | CPU / RAM / disk | Boots alone | Tonight |
|---|---|---|---|---|---|
| Box (GB10, 121 GiB) | Ubuntu 24.04.4 = DGX OS 7.5.0, kernel 6.17.0-1032-nvidia, driver 580.173.02, CUDA 13.0 | 09-04T17:15:10Z, 0 boots since ("10 Sep reboot" premise refuted, `journalctl --list-boots`) | load 0.19 · 44/121 GiB · 11 % · GPU 0 %, 42 °C | 21/22 project user units at boot (Linger=yes); scanner is `WantedBy=multi-user.target`, which the user manager lacks | `hermes update` 19:53:17Z (orphan reset, 2101 commits, 2nd in 5 d); hermes-serve restarted 19:53:47Z and 20:08:46Z; 2 untracked bill files under `realms/admin-billing/inbox/2026-09-04/` |
| Mac (M2 Pro, 16 GiB) | macOS 26.5.2, Darwin 25.5.0 | 16:53:28Z (clean reboot after 12 d) | load 2–3 · 68 % free · 333/926 GiB | 3/7 (tunnel, guard, skhd); herdr, Hermes Desktop, Claude Desktop hand-started | Tailscale "stopped", guard state `stranded`, no alert (transition-only); 6 ssh bridges to the box open at once |

### b) Fleet (7-day window 09-08T20:05Z→09-15T20:45Z; box rows from `journalctl --user -u <unit> --since '7 days ago'`, Mac rows from `herdr agent list/read`, `launchctl print gui/501/<label>`)

| Name | Host | Runtime | State (20:20Z) | Last useful output | 7 d runs / converged / failures |
|---|---|---|---|---|---|
| lucky-loop | box | timer */10, llama3.2:3b | idle, inbox 0 | 20:20:01Z "inbox empty" | 1008 / 2 passes 09-09T01:40Z+01:50Z both converged / 0 |
| artifact-return | box | timer 30 min | idle | 20:05:03Z passes=5 new=0 unchanged | 255 / 11 pushed / 1 (09-11T17:35Z) |
| artifact-return.yml | GitHub | schedule ~6 h | green | 17:09:13Z success | 4/4 today; 20 green, 2 failed since 09-08 (`gh run list`) |
| gates.yml | GitHub | push/PR | green | 20:10:08Z main | 3/3 today |
| nightly-queue | box | timer 01:30Z, `delegate`→`claude -p` | idle | 01:31:43Z: 6 jobs, bills-to-loop 0 new; 2 memo jobs BLOCKED (empty realms) every night | 7 / 7 / 0; 16 delegations (14 scout, 2 foreman) |
| foreman | box | timer 15 min, `claude -p` | "no eligible open task" | 20:05:45Z | 648 / 1 landed (09-10) / 1 push REJECTED at protected path (09-11) |
| infra-watch | box | timer 15 min | HEALTHY 14/14 | 20:05:46Z | 649 / 157 DEGRADED samples, 31 transitions / 9 tracebacks (`git commit` CalledProcessError) |
| polysignal-truth-board | box | timer 15 min | ok | 20:05:52Z obs=23218 | 649 / – / 0 |
| polysignal-feedback | box | timer 03:00Z | ok | 03:00:01Z "No actions taken" | 7 / – / 0 |
| polysignal-scanner | box | daemon | restarted 20:14:47Z on PR #4 code | cycle 1 on the new code complete 20:47:18Z, 0 errors | 223 cycles / 0 tracebacks / 1 SIGKILL on stop timeout; a 422 warning every cycle |
| decide-listener | box | daemon | active since 09-11T05:09Z | heartbeat 20:20:13Z; `seen` = 0 words ever | – / – / 5 poll-failed 09-11 |
| hermes-serve | box | daemon :9119 | active since 20:08:47Z | fleet API mounted 20:08:47Z | 4 restarts / – / 0 |
| hermes-gateway | box | daemon | active since 19:53:44Z, agents 0 | logs feed-gate hook "not allowlisted — skipped" at every start | NRestarts 2 / – / – |
| Hermes cron deploy-check | box | */10, no-agent | ok | 20:10:48Z | ~1000 rows (retention cap) / – / 0 |
| ollama system :11434 | box | daemon | llama3.2:3b loaded 20:07:31Z by this brief's os-layer probe (one prompt, for tokens/s) | 6 loop calls / 7 d | – / – / 26 embedding-length errors |
| ollama-router :11500 + shim | box | daemon | qwen3.6:35b-a3b 22.85 GB pinned | chat probe 200 in 0.09 s | 52 completions / – / 0 |
| cron watchdog-host | box | */5 | FAILING | 20:15:01Z "unit is masked" | 2016 / 0 / 2016 (cumulative 4407) |
| cron gbrain-reap-orphans | box | */5 | never logged | – | 3202 this boot / – / log file absent |
| cron nightly-dream | box | 01:15Z | ok | 01:15:04Z "nothing to do" | 7 (unverified) / – / 0 |
| herdr-server | box | daemon | 0 agents, 6 transcript tails | – | – |
| console / site / shim / docker ×3 | box | daemons | up 11 d | – | console NRestarts 1 (boot crash, retried) |
| herdr server | Mac | hand-started 17:01:22Z | 16 agents: 3 working, 8 idle, 5 blocked | – | – |
| truth (w3:p1) | Mac | Claude Code 2.1.272 | idle (done 20:15Z) | PR #4 merged 20:12:55Z, box restarted 20:14:47Z | 1 task claimed 20:05Z, landed 20:15Z |
| truth-verify (w3:p2) | Mac | Claude Code | idle (done 20:48Z) | VERDICT VERIFIED 20:48Z | 1 verification, 0 refusals |
| commander (w2:p3) | Mac | Claude Code | idle | clock-out 16:52Z | – |
| w2:p4 (this brief's parent) | Mac | Claude Code | working | `89cae0d` pushed 20:10Z (inferred) | – |
| scout · panel · gardener · gardener-verify · architect · worker · w2:p2 | Mac | Claude Code | idle | 09-04T15:12Z · 09-05T15:09Z · 09-05T15:39Z · 09-05T15:42Z · 09-05T14:46Z · 09-08T13:08Z · 09-10 ~11:49Z | 0–1 turns each |
| nightly · nightly-verify · mail · w1:p7 · w1:p8 | Mac | Claude Code | BLOCKED at "trust this folder" for `$HOME` since 17:01Z | none since reboot | – |
| Hermes Desktop | Mac | hand-started 19:54:38Z, v0.21.3 | backend ready 19:54:40Z; fleet plugin `TypeError` 19:52:57Z, 19:54:40Z, 20:08:50Z (0 on ~200 earlier boots) | 8 boots today | Today render UNVERIFIED |
| Claude Desktop | Mac | hand-started 19:34:42Z | 3 embedded Claude Code 2.1.260 + 3 ssh MCP bridges to the box | – | – |
| launchd ssh tunnel | Mac | KeepAlive | up since 20:04:46Z, runs=3 since boot, last exit 255 | 961 "Connection refused" lines (box-side, during hermes-serve restarts) | – |
| tailscale-guard | Mac | launchd 120 s | `stranded`, silent | – | 83 / – / 0 alerts |
| loop-publish (retired fallback) | Mac | launchd 6 h, RunAtLoad false | LOADED at 20:58Z, runs=0 — re-armed by the 16:53Z login; `launchctl bootout` REFUSED to the agent by the classifier 20:52Z → card | 09-09T05:47:36Z `pushed=yes` | fires ≈22:56Z (unchanged), ≈04:56Z (push to public `main`) if awake |
| Hermes cron | Mac | – | 0 executions ever; ticker stopped 16:38Z | – | – |
| loop-factory | Mac | – | dormant | lap 1 CONVERGED 09-08; last commit 09-08T15:32Z | 0 / – / – |

### c) Autonomy KPIs (window as above)

| KPI | Now | Target | Proof |
|---|---|---|---|
| Loop passes / night | 0.29 (2 of 7; 0 in the last 6) | ≥1 | `~/ll-loop/out/loop-runs.json` |
| Items fed / night | 0.29; last fedAt 09-09T01:31Z; cap never hit | ≥1 | `~/.local/state/lucky-loop/feed-from-bills.json` |
| Idle loop ticks | 1006 of 1008 | — | lucky-loop journal |
| Foreman claims / landed | 2 / 1; 646 of 648 ticks "no eligible" | ≥1 landed | `~/brain/logs/foreman.jsonl` |
| Board rows an agent created | 0 of 11 | >0 | `queue/tasks.json` |
| Words received remotely | 0 ever | ≥1 | decide-listener `state.json` |
| Unattended publication | 11 host pushes; PR #1 merged 19:52:03Z, 5 d after opening | merge within 24 h | `gh pr view 1` |
| Heartbeats | 10/10 fresh | 10/10 | `~/brain/tools/heartbeat check` |

**Named cause of the idle nights:** (1) lucky-loop — **empty source**: the only feed is the mail INBOX × a 17-word bill regex; 14 d = 154 messages, 9 with attachments, 1 matched (already fed), 8 unmatched, 6 of them PDF, 0 match by filename (council measure 20:33–20:35Z, counts only). (2) foreman — **rule + no producer**: `eligible()` refuses `host=mac`, both open rows are `host=mac`, only the commander has ever written a row. Neither is "no queue".

### d) Utilization and cost

| Resource | Now | Proof |
|---|---|---|
| Box CPU | idle 99.45–99.67 % per day | `sar` sa07–sa15 |
| Box GPU | 0 % util; 25.3 GB qwen3.6 + 3.4 GB llama3.2:3b + 0.2 GB scanner | `nvidia-smi --query-compute-apps` |
| Local inference | 52 router completions + 6 loop calls / 7 d, $0 | ollama journals |
| Foreman `claude -p` | cost_usd 5.8 per landed task (09-11); "Max absorbs it" is an assertion | `foreman.jsonl` |
| Claude Code volume, September (equivalent API cost — Max is a flat plan) | Mac $459.89 = 259 M tokens, 240 M of them cache reads; box $46.09; per day 09-08 $153.38, 09-09 $25.47, 09-10 $48.31, 09-11 $0.41, 09-12..14 none, 09-15 $25.08 at 20:19Z — this brief's workflow spent 4.21 M subagent tokens after that measure | `npx ccusage monthly --json`, `daily --json --since 20260908`, on both hosts |
| Mac | 15 resumed Claude sessions + 2 desktops + 6 concurrent ssh bridges on 16 GiB | `ps`, `ps … grep 'cloudflared access'` |
| Waste | 288 failing watchdog runs/day; 26 embedding errors; 961 refused tunnel channels; 1006 idle ticks | logs above |

### e) The six needs-you cards, ranked by the cost of Karl's delay (`queue/needs-you.json`, updated 16:50:36Z; 6 open)

| # | Card | Needs his hand | Rule | Breaks in a week | Default/rule removes him? |
|---|---|---|---|---|---|
| 1 | `ntfy-actions-merge-gate-2026-09-11` | checkout master, `merge --no-ff`, `push -o gate=karl`, Mac ff-pull (4 lines as written) | vault pre-receive protects `tests/`; `gate=karl` is the override | master frozen 5 d → 12 d; 32 → ~39 stranded commits; foreman lands nothing; two queue counts disagree | No for this push; YES for the next: rule (d)1 + foreman worktree |
| 2 | `ntfy-alerts-topic-2026-09-11` | paste the alerts topic into the box config | secrets never in a repo | `decide-listener` `seen` stays 0; remote word path dead | No (secret) |
| 3 | `ntfy-phone-2026-09-10` | install the app | physical | same as #2 | No |
| 4 | `merge-artifact-return-pr1-2026-09-10` | close it: `needs-you done <its number>` — PR merged 19:52:03Z by Karl | "only Karl closes" | NEEDS YOU over-counts by one daily | YES: rule (d)2, its `check` already returns MERGED |
| 5 | `rule-classifier-refusal-is-a-stop-2026-09-10` | adopt the rule text | rules are his | next refusal has no rule | Agent proposes; only he adopts |
| 6 | `r1-apple-notes-vault` | move the credential vault (R-1, P0 standing) | — | nothing new; unbounded tail; past `expiry` 09-15 | No |
| 7 | `loop-publish-dearm-2026-09-15` (NEW, tier 1) | paste two lines on the Mac: `launchctl bootout` + rename the plist | rule card: the classifier refused the agent at 20:52Z, a refusal is a stop | fires ≈22:56Z and ≈04:56Z whenever the Mac is awake; a push to public `main` with no merge whenever its gates are green | It WAS the default — the agent was refused; a permission rule for `launchctl bootout gui/501/com.kww.*` would remove Karl next time |
| 8 | `word-shim-hermes-rooms-2026-09-15` (NEW, tier 3) | one word: adopt (October) / defer / reject | rule 5, one architecture change a month | nothing; it is October's candidate | No — it is a word by definition |

Agent's hand, no word — what happened to each tonight: de-arm `loop-publish` → REFUSED by the classifier, carded (#7); scanner `WantedBy=default.target` → board task `polysignal-scanner-unit-default-target` (host dgx, foreman-eligible, vault branch `c48dd8b`); the 5 trust-prompt panes → NOT done (editing herdr's session file under a running server is not small); `tailscale up` → NOT done (the guard's own prescription names a second VPN; which one runs is Karl's, not a default); `gbrain sync --skip-failed` → NOT done (the "blocked" claim carries no proof in the packs; unverified).

## Q1 AUTONOMY — the better-than-Grok-Bot plan

- **Now:** console updated but Today render unproven; room works (truth shipped PR #4 from a board row) with 5 panes needing Enter; runtime 9 timers, 0 failed, GPU 0 %. 0.29 passes/night, 0.14 landed tasks/night, 0 self-made rows; the box has proven start→coordinate→finish once (09-10).
- **Target:** ≥1 pass and ≥1 landed task per night; Karl's only words are repo merges and gate pushes.
- **Gap:** empty bill source (6 unclassified PDFs = Hop 0, his eye); foreman refuses `host=mac`; vault clone 32 ahead of a frozen master so nothing verified can land.
- **Next move:** (1) gauge, not widen — a count-only "N unmatched with attachments (M PDF)" line in the nightly bills job, `realms/*/inbox/` gitignored in the same commit; (2) unstrand — Karl runs card #1, then the foreman checks out into a worktree and `foreman-return-vault-clone-to-master` flips to `host: dgx`; (3) a producer that is not Karl and not an LLM — infra-watch appends a `host: dgx, queuedBy: infra-watch` row when one error string repeats ≥3 runs, ≤1/night. Hygiene: `loop-publish` de-arm is card #7 (refused to the agent); card #4 is Karl's `needs-you done`; log the skipped feed-gate hook.
- **Proof:** the 09-16 nightly capture prints the unmatched count; `ssh dgx-remote 'git -C ~/brain branch --show-current; git -C ~/brain rev-list --count master..HEAD'` = `master` / `0`; a `queuedBy: infra-watch` row plus a foreman claim within 24 h.
- **Recommendation:** gauge intake instead of widening it, give the foreman a worktree, add the deterministic producer; the one thing only Karl can do tonight is `gate=karl`.
- **Dissent (verifier):** a producer raises claims per night, not converged-and-landed per night — the foreman's only landed run still needed a human at the vault gate.
- **Word needed:** `gate=karl`.

## Q2 PERCEPTION & COLLABORATION UI

- **Now:** two rosters, zero overlap. herdr: 16 agents, live state (8/5/2/1), chat = scrollback, no groups, no phone. Hermes: 3 bots, Bot Chat + rooms shipped, 0 rooms ever, last turn 08-27. Today: job rows, no live state; renders on the Desktop UNPROVEN (3 load failures tonight). Struck: "signed ADR amendment 08-28" (no decision file; the rule is binding via CLAUDE.md, unsigned); `hermes kanban` exists, so "excluded by contract" is now a house rule.
- **Target:** one WhatsApp-style place for eyes and hands, drop-in included.
- **Gap:** the target folds hands into eyes; AGENT-OS rule 5 allows one architecture change per month and September is the foreman.
- **Next move (no word):** relaunch the Desktop — ATTEMPTED 20:53Z: `osascript quit` did not end the process (same pid before and after), the renderer re-resolved the backend at 20:53:14Z and has logged no new load failure (count stays 4), so the render is still unproven — Karl opens Today and looks; if it errors, the agent bisects `plugin.js` against `plugin.js.bak-20260915`; an AGENTS NOW block on Today from a 60 s herdr snapshot (name/state/last-done only, `syncedAt` beside it); strip `file/delegation/cronjob/computer_use/kanban` from room members, then one room on one board task whose outcome is a card. Remote channel is DECIDED (ntfy-actions, 09-10): blocks are cards #2 and #1 plus one `--watch` unit the agent adds. Live groups of Claude Code agents = October, on the word.
- **Proof:** `grep -c 'runtime load failed (fleet)' ~/.hermes/logs/desktop.log` stays 4 after a new backend line; `today.txt` shows 16 rows whose states equal `herdr agent list` at the same timestamp; `decide-listener` `seen` 0 → 1.
- **Recommendation:** Hermes Desktop (Today beside Bot Chats) is the one place by the existing rule, while saying plainly it holds none of the 16 agents Karl runs today.
- **Dissent (verifier):** Hermes cannot send keys into a Claude Code pane, so "one place" gives state without drop-in; herdr may need to grow chat and remote instead.
- **Word needed:** `shim` (schedules October only).

## Q3 OS LAYER

- **Now:** box 21/22 project units up 6 s after boot; Mac 3/7; total 24/29 = 83 %. Hand starts: scanner unit target (defect, misses the next boot too); `herdr server` (by design); 5 resumes at the trust prompt (fix cwd, never trust `$HOME`); Tailscale stopped with a silent guard; both desktops (by design); and the reverse — `loop-publish` re-armed by the login although `docs/AUTONOMY.md:88` says unloaded.
- **Target:** box 22/22; Mac stays 3/7 (the interface starts when the human does), 0 blocked sessions, a guard that speaks.
- **Gap:** one unit-file word, one push path, one cwd, one guard rule.
- **Omarchy 4:** NO on both — its package repo answers 404 for aarch64 (curl 20:31:29Z) and both hosts are arm64; the box already is the "agents as citizens" idea. **Below-OS:** BLOCK — CPU idle 99.5 %, GPU 0 %, nothing here compiles CUDA, the 6.14→6.17 + libc6 upgrade already ran 09-04; every failure this boot is user-space.
- **Next move:** the `loop-publish` de-arm is card #7 (the agent was refused); scanner `WantedBy=default.target` is board task `polysignal-scanner-unit-default-target` (host dgx, foreman-eligible); point the 5 panes at a project cwd and teach the guard "stopped + state older than boot"; fix runbook line 3 ("NOT executed" — it ran 09-04T17:15Z) and close the libc6 A/B memory line.
- **Proof:** `launchctl print gui/501/com.kww.loop-publish` → "Could not find service", still after a reboot; `ls ~/.config/systemd/user/default.target.wants | grep scanner`; `herdr agent list` blocked=0.
- **Recommendation:** Omarchy on neither host, nothing below DGX OS; de-arm the publisher tonight (card #7) and fix the scanner target (board task).
- **Dissent (risk):** de-arming removes the only unattended publisher that runs the by-name deny-list with the real names (the Action merely attests it); the box is idle only because the inbox is empty.
- **Word needed:** none.

## Q4 SITUATIONAL AWARENESS EXTRAS

- **(a) Client-visible outputs, 7 d:** 2 redacted passes on public `/loop` (09-09); the host→mirror→Action→PR chain, 20 green / 2 failed runs, PR #1 merged with an Action-authored commit; Today page +2 blocks. Struck: "bot-opened PR #1" (author is Karl; the Action's PR is #2, a closed test).
- **(b) Biggest 72 h risk:** `com.kww.loop-publish` fires ≈04:56Z with a 14 h-old snapshot ≥ its 12 h threshold → `git push origin main` on a public repo, removing the merge, "the ONLY human step left", and a second writer on `data/loop-status.json` 7 min before the Action's run. Control regression, not a leak (gates green, deny-list local). **Owner:** Karl, by the rule card — the commander tried at 20:52Z and the classifier refused; card #7 carries the two lines. The 09-10 word `action` already decided it. Runner-up: the 5 parked panes.
- **(c) Human hops:** merge the artifact PR (keep); Hop 0 (keep); `gate=karl` push (Karl, card #1); card closing (rule); trust prompt (default cwd); Tailscale (agent); phone, topic, mail OAuth (Karl, physical/secret).
- **(d) Rules that now hurt, amended:** (1) "nobody pushes to master until the merge card" → protected-path hunks go alone on `proposal/<topic>`; direct-write commits go to `master` the same tick and the clone checks `master` back out. (2) "Only Karl closes an item" → an item whose `check` exits 0 closes with `closedBy: check`, listed 7 d; past `expiry` = `expired`. (3) "retired and stays as the fallback" → retired = renamed plist; bootout is a pause; re-arming is a Karl word. Run `npm run check:flow` after (3).
- **(e) Karl does not know:** gbrain sync reported BLOCKED since 16:50Z on an oversize review page — vault search answers from a pre-clock-out index (unverified: no pack carries the proof; the box journal for a sync unit is empty; this Mac's gbrain MCP failed to connect this session); the only real bills the loop ever ate sit untracked in the vault clone and one `git add -A` publishes them into history; `data/okrs.json` KR4 0.14 is hand-typed and the O3/KR1 note undercounts; the cron watchdog still holds `sudo systemctl restart ollama`; a dormant third-party API key sits in the Mac's Hermes `.env`; llama3.2:3b loaded 20:07:31Z and hermes-serve restarted 20:08:46Z were BOTH this commander session (the os-layer tokens/s probe; the Today-page install) — the logs name neither, which is its own finding.
- **Recommendation:** Karl de-arms `loop-publish` via card #7 before 04:50Z (the agent was refused); AUTONOMY.md reworded tonight so retired means renamed; unfreeze the vault on `merge` through card #1 as written, then `gbrain sync --skip-failed`; land (d)1–(d)2 on a `proposal/` branch.
- **Dissent (risk):** disarming trades a real local by-name gate for a human click that was five days late on PR #1.
- **Word needed:** `merge`.

## CORPSES tonight

| Corpse | What happened | Proof |
|---|---|---|
| `council --help` | the commander convened a REAL council on the string `--help`: one Opus call and one Codex call, dead at the 120 s timeout; the council has no flag guard; nothing reached the vault | commander pane; council CLI |
| "PR #1 still open" | stale 47 min after the handoff was written; card #4 still open at 20:37Z | `gh pr view 1` |
| "10 Sep box reboot" | never happened; runbook line 3 and the 09-10 clock-in still say pending while the 09-04 handoff says done | `journalctl --list-boots` |
| Gatherer errors | LaunchAgents 12+1 → 11+1; scanner "active since 09-10" → restarted 20:14:47Z; infra-watch tracebacks 3 → 9; journal errors "sshd ×8" → 152/74; "gbrain MCP parked" was a pre-reboot gateway; delegations 16 = 14 scout + 2 foreman | re-runs 20:11–20:22Z |
| Council strikes | Q1 "widen `listing(3)` to 30 d", "~1 bill / 11 days"; Q2 "signed ADR 08-28", herdr-agent-state as roster bridge; Q4 "bot-opened PR #1" | arbiter re-runs 20:34–20:45Z |
| Proof hygiene | `curl localhost:9119/` prints a page carrying a live session token — run with `-o /dev/null`; the gatherer's proof line invited a leak into an agent-read terminal | fleet-dgx |
| Agent misfires | a refused `sudo crontab -l` at 20:14:36Z from an agent session; `herdr agent list` on the box empty while 6 transcript tails run; the box's Hermes install reset onto a rewritten upstream twice in 5 d | box journals |
| Unattributed | who ran `hermes update` on the box 19:53:17Z (Desktop-driven, inferred) | update logs |
| Refused, carded | `launchctl bootout` of the re-armed publisher — the classifier refused the commander (Interfere With Workloads) at 20:52Z; not routed around; card #7 | commander pane |
| Quit that did not quit | `osascript` quit of the Desktop at 20:53Z left the same pid; the renderer re-resolved the backend anyway; Today render unproven | `~/.hermes/logs/desktop.log` |

## DATED PREDICTIONS (falsifiable; proof beside each)

| When | Prediction | Proof |
|---|---|---|
| 09-15 ≈22:56Z | unless card #7 has run: `loop-publish` fires on the Mac if awake and logs `unchanged` (snapshot < 12 h) | `tail -3 ~/.config/loop-publish.log` |
| 09-16 00:28Z → 06:00Z | the status file gains `state: sleeping` + `until` within a minute of the sleep and its mtime advances every 25 min | `ssh dgx-remote 'stat -c %y /opt/loop/lab/.scanner-status.json'` |
| 09-16 by 01:00Z | the box foreman claims `polysignal-scanner-unit-default-target` (host dgx, no not_before) — or logs why not | `~/brain/logs/foreman.jsonl` |
| 09-16 01:00–01:15Z | NO `TRANSITION heartbeats -> UNHEALTHY` in the infra-watch journal (the merged code has run since 20:14:47Z); else the transition again (today: 01:10:03Z) | `ssh dgx-remote "journalctl --user -u infra-watch --since '2026-09-16 00:00' | grep TRANSITION"` |
| 09-16 by 01:45Z | nightly-queue ran; bills-to-loop 0 new; both memo jobs BLOCKED again | `tail -1 ~/brain/logs/nightly-queue.jsonl` |
| 09-16 03:00Z | polysignal-feedback "No actions taken" | its journal |
| 09-16 ≈04:56Z | IF not de-armed and the lid is open: `loop-publish` pushes to public `main` (falsifies the merge-is-last-step rule); if de-armed: no line | `git log origin/main -1`; the log above |
| next scheduled runs (≈23:00Z, ≈05:00Z) | artifact-return.yml green, `new=0`, no PR unless the host stages a fresh snapshot | `gh run list --workflow artifact-return.yml --limit 2` |
| until card #1 runs | the bare's `master` stays at `786537a` (09-11T05:00Z); the box clone stays on the foreman branch at 34+ ahead, growing ~1/day | `ssh dgx-remote 'git -C ~/brain rev-list --count origin/master..HEAD'` |
| until Enter is pressed | the 5 panes stay blocked; Tailscale stays stopped; the guard stays silent | `herdr agent list`; Tailscale status |

## THE ONE MORNING CHECK COMMAND

```
dgx-today --brief && ~/brain/tools/heartbeat check && ssh dgx-remote 'git -C ~/brain branch --show-current; git -C ~/brain rev-list --count origin/master..HEAD'
```

Expect `NEEDS YOU: 6` or fewer, `BOX: OK`, `KR-0 PASS 10/10`, and the last two lines `master` / `0`. `foreman/ntfy-actions-remote-words` / `32` or more means card #1 is still waiting. If `dgx-today` prints nothing, the Desktop's tunnel is down — check `launchctl print gui/501/<tunnel label>` first.

## SHORTLIST (next session)

1. Clock-in proof of the night: no UNHEALTHY transition; status-file beats from 00:28Z; the foreman's claim of the scanner unit task.
2. Cards #1 (`gate=karl`, unfreezes the vault; also closes `foreman-return-vault-clone-to-master`), #7 (two lines), #4 (`needs-you done`). Then the rule words: #5 and #8.
3. Q1 move 1 — the count-only "N unmatched with attachments (M PDF)" line in the nightly bills job, `realms/*/inbox/` gitignored in the same commit; Q1 move 3 — the infra-watch producer, on a `proposal/` branch.
4. Q2 — the AGENTS NOW block (herdr state as shape, `syncedAt` beside it); Desktop render proof from `desktop.log`.
5. Q4(d) rule amendments (1)–(3) as a `proposal/` branch for Karl's gate.
6. Stale declared numbers in `data/okrs.json` (O3/KR1 note, KR4 hand-typed 0.14): derive or correct, never raise.

## REDACTION NOTE

Kept generic: "the box", "the Mac", `ssh dgx-remote` (an ssh alias, already in the 09-15 morning handoff). No topic names, key paths, account ids, bill filenames or fingerprints. The launchd label `com.kww.loop-publish` is already versioned in `deploy/`.
