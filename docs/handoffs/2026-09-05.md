# Handoff — 2026-09-05

Written for the next session. **Every claim here is a hypothesis.**

REDACTION: this file is in a PUBLIC repo. No hostnames, tunnel names, key fingerprints,
token values, auth URLs or personal names appear below, by rule.

Session shape: Karl in transit, two hours, one commander session briefing one agent per
herdr workspace, plus one independent verifier per worker. New commander ritual:
`/clockin` and `/clockout` are global skills; the role lives in files, not in a window.

## SHIPPED (with numbers)

- **First real night on iCloud mail, unattended.** morning-brief ok 8 s (page + mp3 in the
  narrator voice), PolySignal week report on the right week at last (W35, made 408, correct
  197), inbox-drafts ok 7 s (0 drafts: 2 unread needed no reply, 5 automated senders
  skipped), bills-to-vault ok 5 s (34 scanned, 0 new, 1 already filed). Nothing sent.
- **AGENT-OS page in the vault** (`docs/AGENT-OS.md`, ~190 lines): three layers, the vault
  as message bus, roster of 15 automations, 9 standing rules, grand goal + autonomy plan per
  project. Corrected once the same day by the architect's findings (below).
- **ADR-4 drafted** in the platform repo, after ADR-3, status DRAFT awaiting signature, with
  a tripwire: if the foreman is still absent on 2026-09-30 the ADR reverts to "planned".
  Verified by an independent agent: commit on origin/main, section table row present.
- **PolySignal experiment ladder, 7 rungs**, on branch `plan/autonomy-2026-09-05`; rung 1
  (log side, fill, bid, ask, spread, fee, size, quote time on every prediction and paper
  trade; truth board computes friction win rate from that file alone) on branch
  `lever/log-friction-fields`, 565 tests green per the agent; edge diagnosis: row-level edge
  −2.6 pp, market-level −6.0 ± 4.4 pp over 364 unique markets; prices are not stale (median
  gap to the CLOB mid = 0); most likely cause: the observation up/down-tick base-rate source
  at −5.0 pp on 2,451 rows. Main untouched. Independently verified 5/5: branches on origin, 565 passed under the CI invocation (one pre-existing failure identical on main), the six changed files carry side/fill/bid/ask/spread/fee/size, the box unchanged.
- **Vault entity pages** for the box, the platform repo and the mail-triage tool, every fact
  with a source path, zero secrets (verified by grep); `docs/AUTONOMY-vault.md` with three
  recurring board tasks. Orphans 16 of 192 pages (rose from 12 because 12 new pages landed
  mid-session); dangling targets 8, none a system name.
- **Briefs as files.** `queue/briefs/` in the vault holds the foreman, feeder and architect
  briefs. Two of them are not launched (see OPEN DECISIONS).

## PRINCIPLES

1. **Briefs are files, not shell strings.** The commander's safety classifier refused three
   inline briefs; the vault copy is reviewable, replayable and identical on both hosts.
2. **No agent verifies its own work.** Every worker this session got a fresh read-only
   verifier; two corrections to the commander's own page came from that step.
3. **The role is the files.** Long windows degrade; `/clockin` + `/clockout` make the next
   commander start from the Today brief, the newest handoff, the task board and this file.
4. **A grand goal needs an acceptance number per rung**, or the foreman cannot merge alone.

## OPEN DECISIONS (all in the needs-you queue)

1. **Launch the foreman brief** — one line from Karl's terminal (the commander is not allowed
   to instruct unattended pushing agents): tell the `nightly` agent in herdr to read and
   execute `queue/briefs/2026-09-05-foreman.md`.
2. **Launch the feeder brief** — same, for the `panel` agent and
   `queue/briefs/2026-09-05-feeder.md` (Mac-side publisher every 6 h, gates first).
3. Merge or reject `lever/no-bullish-below-0.5` (30 d accuracy 54 → 70 %; edge still
   negative; gate stays closed either way). Recommendation: merge.
4. Ratify or revert the accuracy dashboard on vault master — verified: no override, the gate
   accepted a commit already on a proposal branch by design. Recommendation: ratify, then
   tighten the hook so protected-path merges need the human override flag.
5. ElevenLabs Starter, yes or no (free tier ≈ 12 morning briefs).
6. A repo-scoped deploy key on the box for this repo, `loop/`-prefix branches only, replicating
   the pattern that already exists for the PolySignal repo — yes or no.
7. R-1, the Apple Notes migration (hand job, one step per sitting).

## IN FLIGHT

- Nightly queue 03:30 as before (morning brief, week reports, iCloud mail jobs).
- Nothing else started by this session is running. Six herdr agents idle.

**THE ONE MORNING CHECK COMMAND**

```
dgx-today --brief
```

Expect `NEEDS YOU: 7 item(s)` (fewer as Karl decides), `AGENTS: … 0 failed`, `BOX: OK`. If
the foreman brief was launched: `python3 -c "import json;[print(t['status'],t['id']) for t in
json.load(open('/Users/kww/brain/queue/tasks.json'))['tasks']]"` shows the seeded task as
`verified` or `blocked` with a reason.

## DATED PREDICTIONS

- **2026-09-06 03:35**: inbox-drafts writes ≥ 1 draft into iCloud Drafts (59 unread at
  clock-in; the router's YES/NO on "asks Karl something" has said NO to everything so far).
  Falsified if 0 drafts again — then the question classifier is too strict.
- **2026-09-08**: if rung 1 is merged and deployed, ≥ 95 % of new resolved rows carry a booked
  fill and the 7 d friction window is non-empty. Falsified by the truth board's own line.
- **2026-09-30**: ADR-4 tripwire — foreman present and ≥ 1 task `verified` per week, else
  the ADR reverts to planned.

## SHORTLIST

Constraint: at least half must produce something an outsider can see, or move a D grade.

1. Launch foreman + feeder (Karl, two lines) → first verified board task → provenance line
   on /loop (outsider-visible).
2. Merge rung 0 and rung 1 → truth board shows friction win rate → /war number moves.
3. Rung 2: one call per market (kills the ×10 repetition inflation).
4. Tighten the vault gate for protected-path merges.
5. Hermes voice: Starter plan, then the Today page read aloud on the phone.

## CORPSES — claims that died on contact today

- "The DGX never holds a GitHub credential" (commander's AGENT-OS page, first draft) — it
  holds one repo-scoped deploy key for the PolySignal repo, branch prefix `loop/` only;
  found by the architect, verified by an independent agent. Page and queue corrected.
- "/opt/loop is hard-reset every 10 minutes by cron" (memory since May) — that cron line no
  longer exists; the crontab has a watchdog, the 03:15 dream, a 03:45 mirror refresh and an
  orphan reaper. Memory corrected.
- "queue/tasks.json created 2026-09-05" (the foreman brief) — not yet; the brief is
  unlaunched. ADR-4 marks the foreman PLANNED, with a tripwire.
- "Orphans 12" — 16 by clock-out, because this very session added 12 pages; the metric
  moves with activity and needs the weekly task, not a one-off.
- "Prices might be stale" (a hypothesis in the quant brief) — median gap to the CLOB mid is
  zero across 45 same-hour predictions; dropped.
