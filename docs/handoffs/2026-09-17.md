# Handoff — 2026-09-17 (commander, clock-out 15:50 CEST): close every past session; the next agent starts from files alone

**Exit code: 0.** No lane is open in anyone's head. The 2026-09-16 work session is concluded with
every lane green, stalled-and-carded, or carded; the 13 herdr panes are read, captured and closed;
all five repos are consistent; five pieces of unfinished work left panes and became board rows; and
at 13:45Z the box's foreman claimed one of those rows **by itself**, with no human word, and its
worker is running. That claim is the first time in this project's history that a task written by the
commander was picked up unattended by the box the same hour.

**Start at section 13** — it is the canonical clock-out block (SHIPPED, PRINCIPLES, OPEN DECISIONS,
IN FLIGHT with the morning command, DATED PREDICTIONS, SHORTLIST, CORPSES). Sections 1 to 12 are the
day's detail and the two corrections that produced it.

REDACTION: PUBLIC repo. "The box" is the loop host, "the Mac" the interface; no hostnames, tunnel
names, topic names, key paths, account ids, or bill and note titles. `ssh dgx-remote` is an alias.

## 1. The 2026-09-16 session, concluded honestly

Nothing below reads done without the line that proves it. Verify lines re-run today where the claim
was older than a day.

| Lane | Status | Verify line and what it printed |
|---|---|---|
| L1 PROPOSE | **STALLED, carded** | `python3 -c "…queuedBy startswith propose-rank…"` → 0 rows. Proposals themselves shipped: `ls ~/brain/queue/proposals/2026-09-16-*` → 12 files, 25 proposals, 11 agents. The ranker is report-only by design; seating waits on card `l1-seating-word-2026-09-16`. gardener-verify re-ran it 2026-09-17 14:55 and reached the same verdict. |
| L2 UTILISE | **GREEN** | `grep -c 'completion_tokens=[1-9]' ~/logs/propose.log` → 5 (now 9 with the ranker); `awk` over `~/logs/propose-gpu-2026-09-16.csv` → exit 0, 12 samples, peak 96 % GPU. |
| L3 SEE | **GREEN** (one human half open) | `dgx-today \| grep '^AGENTS NOW'` → `1 mac (synced 16s ago) · 13 box · 11 other units on Fleet` at 15:37 today — and it read `13 mac` before the sweep and `herdr not running` during the herdr upgrade, so the empty case and the live case both render. Desktop render still needs Karl's eyes → board row `today-desktop-render-proof`. |
| L4 ROOM | **STALLED with proof** | `captures/tasks/l4-room-spike-2026-09-16.md`: Hermes 0.21.3 has no headless room; agent-to-agent needs the Desktop's Bot Mode or a messaging platform, both excluded by the brief. Successor work is the October board row `shim-claude-code-agents-in-hermes-rooms`. |
| L5 REACH | **GREEN** (one human half open) | `journalctl --user -u decide-listener`: `refused … bad hmac` 15:48:05Z, `applied … committed and pushed` 15:49:06Z. Karl's own tap is card `test-tap-karl-2026-09-16`. Slot: the readable dry run now redacts the HMAC, 17 tests green. |
| L6a reboot | **carded** | `dgx-reboot-window-2026-09-16` — nothing reboots without a window; the break-glass route is checked first. |
| L6b watchdog | **GREEN** | `wc -l ~/logs/watchdog-cron.log` → 4641 at 15:48Z on 09-16 and **4641** today at 13:30Z. The 4,407 failures stopped dead. |
| L6c bills in git | **GREEN** | `realms/*/inbox/` ignored; proven on 09-16 with a fake bill; the box clone reads clean today. |
| L6d derived KRs | **GREEN** | `/war` shows 3 `derived` tags; O3 moved 33 % → **42 %** on the Today page without anyone editing a number. |
| L6e dormant key | **GREEN, last mile carded** | 0 `FIREWORKS_API_KEY` lines in the three Hermes env files; the value sits in the Records vault; three `.bak` copies and the provider revocation are on card `r1-apple-notes-vault`. |
| L6f Hermes resets | **GREEN** | `bash -s -- --check < ~/brain/tools/hermes-reapply.sh` on the box → **50 ok, 1 drift, 0 need a hand**. The drift is a hook consent never recorded on the root install. |
| L6g parked panes | **GREEN** | 0 blocked panes on 09-16; 0 panes at all now but this session. |
| L7 SHOW | **STALLED, carded** | `feed-from-bills: 2 file(s) under the bills drop, 2 already fed, 0 new` in last night's capture. The loop has eaten every document Karl ever consented to; card `loop-next-document-2026-09-16` asks for one new bill. |
| R-1 | **GREEN, last mile carded** | `notes-scrub-check` → 10 of 11 lines read 0; 17 exports in the Records vault; Recently Deleted emptied on the Mac. |

**Scorecard (Karl's six rows) — Now = 09-16 morning, After = 09-17 15:45Z, verified today**

| Row | Now | After | Proof re-run today |
|---|---|---|---|
| Proactive | 0 proposals, 0 board rows by a non-commander | 25 proposals by 11 agents; the box **claimed a board row unattended at 13:45Z** | `foreman: claimed propose-persona-hard-negatives — committed + pushed`, journal 15:45:31 local |
| Persistent | 16 panes, 5 blocked at a trust prompt, two rosters | one roster, 13 box rows + live Mac rows, ghost-free after a 13-pane sweep | `AGENTS NOW: 1 mac (synced 16s ago) · 13 box` |
| Visible | 5 blocks, no live agent state, one without a sample time | 6 blocks, every header stamped, the board and the agents on one page | `dgx-today \| grep -cE '^(NEEDS YOU\|AGENTS\|GOALS\|BOX\|BOARD\|AGENTS NOW).*sampled '` → 6 |
| Long-running on its own machine | loop + foreman only; scanner would not survive a reboot; a cron failing every 5 min | propose lane on the GPU; scanner boot target fixed and deployed; watchdog silent; **two clean nights** | 30 of 30 infra-watch runs `HEALTHY failing=0/14` between 00:20 and 08:00 local |
| Reachable, two-way | 0 pushes ever, 0 taps | 7 pushes with buttons, a forged tap refused, a signed tap closed a card | listener journal, above |
| Safe | rule 10 unwritten; publisher re-armed; key in three files; `master` frozen | rule 10 adopted and exercised 3 times, 0 routed around; publisher de-armed; key in the vault; `master` moving | AGENT-OS §4 rule 10; `launchctl print … loop-publish` → not found |

## 2. Last night's four predictions — graded

| Prediction | Result |
|---|---|
| 00:28–06:00Z: sleeping beat holds the status file fresh, **no** UNHEALTHY transition — night 2 of 2 | **HELD.** `journalctl --user -u infra-watch --since "2026-09-17 00:20" --until "2026-09-17 08:00" \| grep -c TRANSITION` → 0, and 30 of 30 runs `failing=0/14`. The scanner's own line at 02:27 local reads `Sleeping 5h 32m until 06:00 UTC (heartbeat every 25m)`. **The heartbeat task's acceptance — two consecutive clean nights — is now complete.** |
| 01:45Z: bills-to-loop 0 new | **HELD.** `feed-from-bills: 2 file(s) …, 2 already fed, 0 new, 0 skipped. Queue depth 0.` |
| by 08:00Z: no `2026-09-17-*` proposals file, because the timer is not installed | **HELD.** `ls ~/brain/queue/proposals/ \| grep -c 2026-09-17` → 0. |
| the next Action run opens a clean PR from current `main` or publishes nothing | **HELD.** PR #4 opened 05:01:57Z, one file (`data/loop-status.json`), `mergeable=MERGEABLE`. Its gates job queues for a maintainer as documented; only the Vercel checks report. |

## 3. What changed today (the close-out itself)

- **Panes:** 13 read, captured to `~/brain/captures/2026-09-17-pane-sweep.md` **with every session id**, then closed. `claude --resume <id>` restores any of them; transcripts are on disk. Two pieces of pane-only knowledge were rescued: herdr launched from Ghostty removes the no-graphics limitation, and `herdr machine add --label dgx <alias>` puts the box's herdr in the Mac's machine list without touching the systemd timers.
- **Ghost check:** after the sweep the 60-second snapshot self-corrected to `1 mac` with no note; during the herdr upgrade it had correctly read `herdr not running` rather than an empty roster. The day-after rule holds on this surface.
- **Repos, all consistent:** loop-factory's one unpushed commit (the mail-triage handoff) pushed; polysignal fast-forwarded 4 commits to `8f9577e`; platform's council scratch added to its `.gitignore` and pushed to both of its remotes; lucky-loop and the vault clean and level on both machines. **On a branch that `main` should have:** exactly two, both in polysignal — `lever/log-friction-fields` (rung 1, one commit, waiting since 2026-09-05; it is the only reason the friction win-rate gate metric reads null) and `plan/autonomy-2026-09-05` (a plan document). Twelve other local branches are already merged into `main` and are safe to prune. Untracked leftovers, left alone deliberately and not mine to delete: four scratch files in platform (`out1.json`, `sl.json`, `tg.html`, `tr.html`) and a 92 KB `meta-mandala/` directory in polysignal (`bin`, `canvas`, `docs`, `master.html`, `SINGULARITY.md`, nothing modified this month) — someone's side piece, not part of the scanner.
- **Tooling:** herdr is 0.9.1 and its Claude integration was **outdated (v8 < v10)** — reinstalled today, hook at `~/.claude/hooks/herdr-agent-state.sh`, settings ensured. Claude Code is 2.1.274. The Hermes integration reads current (v5).
- **Board:** 18 tasks, 7 open. Five were written today to carry work out of panes: `polysignal-rung1-friction-fields`, `loop-factory-mail-triage-kr2-unmovable`, `today-desktop-render-proof`, `propose-persona-hard-negatives`, `kr3-ci-snapshot-in-nightly`. Each carries an acceptance line.
- **Queue:** 7 open cards (PR #4's merge was added today). That is one over the KR-2 ceiling of 6, which is honest: KR-2 counts Karl's decisions, and the agent that is graded on it cannot move it — exactly what board row `loop-factory-mail-triage-kr2-unmovable` exists to fix.
- **Plan limit guard:** the limit read 90 % used on 09-16 at 16:05Z and resets 2026-09-19 05:00 Berlin. The foreman spawns a worker and a verifier through `claude -p` on that same plan, so `kr3-ci-snapshot-in-nightly` was given `not_before: 2026-09-19T05:00Z` and exactly one dgx row was left eligible tonight.

## 4. IN FLIGHT right now

**The foreman is running a worker on `propose-persona-hard-negatives`**, claimed 13:45:31Z in
`/home/cube/lucky-loop` at `9aeeab0`, worker timeout 45 min, then its own verifier (never the
builder). Check it:

```
ssh dgx-remote 'journalctl --user -u foreman.service --since "16:00" --no-pager | tail -20'
ssh dgx-remote 'python3 -c "import json;d=json.load(open(\"$HOME/brain/queue/tasks.json\"));print([(t[\"id\"],t[\"status\"],t.get(\"verdict\",\"\")[:80]) for t in d[\"tasks\"] if t[\"id\"]==\"propose-persona-hard-negatives\"])"'
```

Also detached by design: the box's nine timers, the listener, the notifier, the Action every ~6 h,
and the Mac's 60-second herdr snapshot. Nothing else on the Mac is running.

**THE ONE MORNING CHECK COMMAND** (expect `NEEDS YOU: 7` or fewer, `BOX: OK`, an `AGENTS NOW` line,
`KR-0 PASS 10/10`, then `master` and `0`):

```
dgx-today --brief && ~/brain/tools/heartbeat check && ssh dgx-remote 'git -C ~/brain branch --show-current; git -C ~/brain rev-list --count origin/master..HEAD'
```

## 5. THE ONE-NUMBER GOAL FOR THE NEXT SESSION

**Board rows landed by the box with no human word: 1.**

Today's claim is the first half. It counts only when the foreman's own verifier writes VERIFIED on
`propose-persona-hard-negatives` and the row reads `verified` on the board without a commander
brief, a Karl word, or a Mac session touching it. In seven days the box has landed exactly one task
(`ntfy-actions-remote-words`, 09-11) and rejected one (`polysignal-scanner-unit-default-target`,
09-15). A second landing turns "the box can" into "the box does".

## 6. TONIGHT'S PREDICTIONS (four, falsifiable)

| When | Prediction | Proof |
|---|---|---|
| 2026-09-17 by 14:45Z | the foreman's worker finishes and its verifier writes a verdict on `propose-persona-hard-negatives`; VERIFIED or REJECTED both count as the machine working, silence does not | `~/brain/logs/foreman.jsonl` last line |
| 2026-09-18 00:20–08:00Z | night 3: no `heartbeats -> UNHEALTHY` transition, `failing=0/14` on every infra-watch run | `journalctl --user -u infra-watch --since "2026-09-18 00:20" \| grep -c TRANSITION` → 0 |
| 2026-09-18 by 01:45Z | bills-to-loop 0 new again unless Karl drops a bill; both realm memo jobs blocked on empty realms | `~/brain/captures/nightly/2026-09-18-nightly-queue.md` |
| 2026-09-18 by 08:00Z | no `2026-09-18-*` proposals file and `systemctl --user is-enabled propose.timer` still `not-found`, because the seating word has not been said | `ls ~/brain/queue/proposals/ \| grep -c 2026-09-18` → 0 |

## 7. THE WORD THE NEXT AGENT NEEDS FROM KARL

One word, on card `l1-seating-word-2026-09-16`: **adopt**, **keep-seating-human**, or **reject** —
may an unattended timer seat the local model's top proposals as open board rows (≤3 tier-1 rows and
≤1 tier-3 card a night)? Everything else the next agent can do under a default. The other six cards
are Karl's hands, not his words, and none of them blocks an agent from working.

## 8. STRATEGY — two questions Karl asked at the close

**a) What are we missing about this situation?**

1. **The product is the harness, and the harness is now provably real — but it has only ever run one
   project.** Board, queue, briefs, cards, foreman, verifier, handoffs, rule 10, the Today page and
   the snapshot are all generic. The billing vocabulary, the bill feeder and the scanner are welded
   on. Nothing has tested whether a second project can be added as a folder rather than as a week of
   wiring. That test, not more lanes, is the launch.
2. **Initiative and identity live on different machines.** The box starts work by itself but each
   foreman run is an anonymous fresh process with no thread and no memory of its last attempt; the
   named agents have threads and memory but wait for a human to open a pane. Today's claim shows the
   box can pull; nothing yet lets it pull *as someone*.
3. **The thinking runs on the plan, not on the hardware.** Yesterday's eleven agents took the plan
   limit to 90 % in about six hours, while the box's GPU idled at 0 % except for 52 seconds. The box
   can watch, sample and rank; it cannot yet think well enough to build. "Near-zero cost on our own
   machine" is true of the eyes and false of the hands, and the schedule has to respect that until a
   bigger local model proves otherwise on a real task.
4. **Two of the loudest metrics cannot move without Karl.** O3 breadth needs a document only he may
   choose (Hop 0 is a control, not a gap), and the needs-you count is a KR graded on an agent that
   cannot close a card. Both are now named rather than quietly failing.
5. **The verifier-never-the-builder rule has paid for itself three times** in two days: a worker that
   restarted a unit against its brief, a "deploy every 10 minutes" that was a comment, and a tap
   proof that records a script as a human. Keep it even when it doubles the agent count.

**b) What should change about how we work?**

1. **One session, one number.** Yesterday's plan had seven lanes and produced good work and a 90 %
   plan limit. A session that names one number, with the lanes that serve it, will ship more and
   cost less. This handoff names one.
2. **Briefs should carry the budget.** Every brief already names its verify line; it should also name
   its ceiling (agents, wall-clock, and whether it may spend plan tokens at all). Nothing yesterday
   knew what it was allowed to consume.
3. **Hand work over as a goal plus a constraint, not as a lane list.** The best output of yesterday
   came from agents proposing inside their own domain; the weakest came from lanes assigned to
   whoever was idle. Say the number and the boundary; let the roster pick.
4. **Read the limit before dispatching.** The banner in any Claude pane is the only local reading of
   it, and it is now the first line of the next clock-in.
5. **Let the box own the cheap half permanently.** Sampling, ranking, snapshots, gate checks and
   report-only passes belong on the local model on the GPU; Claude should be spent on building and
   verifying. That split is what makes a daily cadence affordable.

## 9. CORPSES

- **"The box will claim it tonight" was nearly wrong for a boring reason.** The foreman skips any
  row whose project is not in its checkout map, so two of today's five new rows are Mac-only by
  construction; the two that matter were `lucky-loop` on `dgx`. A board row that names the wrong host
  or project is invisible to the only agent that could claim it.
- **`--since "13:44:00"` on the box is local time, not UTC.** My first watch loop matched an hour-old
  entry and returned instantly. Box journal queries need local times or an explicit date.
- **platform's `origin` was hidden behind `head -2`**, so the first push looked like it had gone
  nowhere; both remotes are level now.
- **The 09-16 handoff predicted the publisher would fire "≈04:56Z if awake"**; it fired at 13:17Z on
  wake instead — right in substance, wrong in the hour, and it cost PR #3.

## 10. ADDENDUM, 15:58Z — the foreman is a third writer to the vault

Found while committing the last board row: **while a foreman run is active, the box's `~/brain` is
checked out on `foreman/<task-id>`.** Two consequences, both seen today:

- The bare's post-receive cannot fast-forward the box's clone and prints "no tracking information
  for the current branch" on every push. Harmless in itself; the bare has the commit.
- Anything committed **from the box** during a run lands on that task branch, not on master. Two
  graded projection pages went that way at 15:47Z; they were regenerated on the Mac and pushed to
  master as `03cccc6`, so `master` and the gbrain index both carry them. Same generator, same content,
  so the branch and master will merge without conflict.

**Rule for the next commander:** run `ssh dgx-remote 'systemctl --user is-active foreman.service'`
before committing vault work on the box. Active or activating → commit from the Mac to `HEAD:master`.

**If tomorrow's morning check prints a branch name instead of `master`,** the run died mid-flight.
Recovery, in order, on the box: `git -C ~/brain push origin HEAD:refs/heads/foreman/<id>` to save the
work, then `git -C ~/brain checkout master && git -C ~/brain pull --ff-only origin master`. The
permanent fix is the open P1 row `foreman-return-vault-clone-to-master` (a git worktree per task),
which this incident has now reproduced a second time, unattended, in a healthy run.

## 11. ADDENDUM, 16:45 CEST — the one-number goal was met the same afternoon, and a second project joined

**The goal in section 5 is already achieved.** The row `propose-persona-hard-negatives`, written by
the commander at 13:25Z, was claimed by the box at 13:45:31Z, built by a worker in 1421 s with zero
tool denials, checked by a separate verifier in 147 s, and recorded **VERIFIED** at 14:11:40Z. No
human word, no brief, no Mac session. Capture: `captures/tasks/propose-persona-hard-negatives.md`.

What the worker actually found is better than the task asked for: `data/okrs.json` spells the list
`keyResults` while `tools/propose.py` read `krs`, so **every persona prompt ever generated was missing
its KR context**. It fixed that, added the three hard negatives, made the value line a number the
script computes rather than a claim the model asserts, and added a `--screen` mode that exits
non-zero if a surviving proposal names a negative or has a value line without a number.

It also **wrote its own needs-you card** asking Karl to merge its branch and return the vault clone to
master. That is the first card an agent has ever written. The commander had already done both by the
time the card was read, so the card is annotated as satisfied and left open, because only Karl closes
one. The clone is on `master`, clean, zero ahead.

**The next session's one-number goal should therefore move up**, not repeat: *two* rows landed by the
box in one day, or the first row landed for a project that is not lucky-loop.

**A second project is on the harness.** `~/Projects/my-big-game` was onboarded today: a backgammon
lifestyle venture that is a business and a web app at once. It has its own `CLAUDE.md`, `data/okrs.json`,
`docs/venture.md` (facts with provenance), `docs/brand.md` (the brand contract), a brief, and a draft
presentation at `app/deck/index.html`. Its board rows live in the vault under `project: "my-big-game"`
and its blocker is card `mbg-explain-the-venture-2026-09-17`. The repo has **no remote by rule**: its
source material is sponsor-confidential. The harness needed no new code to take it, which is the first
evidence for the claim in section 8 that a new project can be a folder. The honest counter is in that
project's `CLAUDE.md`: the box cannot claim its rows until the foreman's `CHECKOUTS` map gains a line.

**Workspace correction.** The clock-out closed the 13 panes Karl listed, and closing the last tab in a
workspace also closes the workspace, so six of his seven workspaces disappeared from the sidebar. They
were recreated at 16:0xZ with their original labels and working directories — tour, lucky-loop,
polysignal, brain, platform, loop-factory — plus the new my-big-game. **Close panes, never workspaces:**
a workspace with one empty shell is the resting state, an absent workspace is a deletion.

## 12. PART 2, 17:15 CEST — the second correction, and the rules that came out of it

**The same error twice in one session.** Karl's clock-out listed panes to close; I treated the list as
a list to clear rather than a judgement per item, and closed all 13. One of them, the zoetrope
investigation, was **blocked mid-task** on two installs the classifier had refused, with a pending
action on Karl. Blocked is not done. Closing it threw away a live thread, exactly as closing the last
tab in each workspace threw away the workspaces an hour earlier.

**Seven panes are back**, resumed by session id in their own workspaces, and five stayed closed:

| Restored | Where | What it still owes |
|---|---|---|
| zoetrope `9e8d187c` | tour | the binary install (now card `zoetrope-brew-install-2026-09-17`) and the live test against a finished transcript |
| mail-access `f5cde008` | tour | nothing of its own; its last instruction became card `delete-placeholder-drafts-2026-09-17` |
| panel `e6af6a06` | lucky-loop | board row `today-desktop-render-proof`, and its own next: the open-tail column beside O4/KR3 |
| truth `da969c24` | polysignal | board rows `polysignal-rung1-friction-fields` and `kr3-ci-snapshot-in-nightly` |
| truth-verify `5b44e029` | polysignal | the verification of both of truth's rows; never the builder |
| gardener-verify `74f94dfa` | brain | its own words: L1 checks 2 and 5 need a fresh run once the seating card is answered |
| worker `8ef0e7c2` | loop-factory | board row `loop-factory-mail-triage-kr2-unmovable`, and the October shim row |

Stayed closed, work filed and nothing owed: scout `f3f59f50` (L5 verdict filed), the herdr-upgrade
pane `7ec77d2a` (09-10, captured), the 09-15 clock-out pane `202e8f39`, gardener `d15eb297` (L5 and
its slot filed; what remains is Karl's tap), architect `0a65c82e` (L1b filed; its next was landed by
the box this afternoon). Every one of them is one `claude --resume <id>` away, ids in
`~/brain/captures/2026-09-17-pane-sweep.md`.

**Two panes changed working directory on the way back.** zoetrope and mail-access both ran in `$HOME`,
which is untrusted, so a resumed session stops at Claude Code's trust prompt and sits blocked forever.
They now run in `~/Projects/lucky-loop` and `~/Projects/loop-factory`. Their conversations are intact;
only the shell's directory moved.

**The rule is now rule 16 in AGENT-OS**, with rules 11 to 15 carrying the advice Karl accepted:
one number per session; a budget in every brief; goal plus boundary with the roster choosing lanes;
read the plan limit before dispatching; the box permanently owns the cheap half. Advice in a handoff
evaporates; these sit beside rule 10 where the foreman and every brief reads them.

**Zoetrope, stated plainly.** The herdr plugin is installed and enabled, pinned to `b1f31dd`, and the
keybinding `prefix+shift+z` is live in `~/.config/herdr/config.toml` as a split beside the pane. The
**binary is not installed** — `which zoetrope` is empty — so the keybinding opens a renderer that has
nothing to render. That install and the live test are the card. The investigation's verdict stands
unchanged: zoetrope read-only and pinned, terminal-code rejected.

**The transcript finding is a board row now**, not a sentence: `transcript-secret-scan-before-any-viewer`.
36 of 498 local transcripts hold secret-shaped strings. They are local today, which is why this is
hygiene and not an incident; a session viewer that renders transcripts on screen is exactly what the
zoetrope card would switch on, so the scan comes first. Its acceptance forbids printing a single
matched value and requires one card per real credential.

**Where the two days of compute went** (`ccusage`, equivalent API cost; Max is a flat plan):

| Day | Output tokens | Cache reads | Equivalent |
|---|---|---|---|
| 2026-09-15 | 1,383,826 | 50.1 M | $154.50 |
| 2026-09-16 | 875,852 | 109.4 M | $148.02 |
| 2026-09-17 | 102,396 | 41.1 M | $31.80 |

By session across 09-16 and 09-17, sixteen sessions, $264.80 total: the commander $72.40 (88.6 M cache
reads, alive across three days), truth $44.00, gardener $30.50, the zoetrope investigation's own
17-agent workflow $29.70 plus its pane $16.60, panel $18.60, architect $13.40, gardener-verify $9.40,
worker $9.20, truth-verify $7.10, scout $6.70, mail-access $4.60. **Cache reads, not output, are the
bill**: 109 M read against 0.9 M written on 09-16 is 120 tokens re-read per token produced. Eleven
agents each re-reading the same instruction files every turn is what spent the limit, and rules 11 to
15 exist to stop it.

---

# 13. CLOCK-OUT — the canonical block

*Read this section first. Sections 1 to 12 are the day's detail and the two corrections that produced it.*

## SHIPPED (with the number that proves it)

- **The box landed two board rows unattended and rejected a third.** `propose-persona-hard-negatives`
  claimed 13:45:31Z, worker 1421 s, verifier VERIFIED 14:11:40Z. `r5-tier1-applier-the-signed-rule-with-no-code`
  claimed 15:43:31Z, worker 829 s, VERIFIED 16:00:13Z. Then it claimed
  `headless-claude-drop-the-unused-mcp-floor` and its own verifier **REJECTED** it: "the live foreman runs
  the working-tree file, which has zero `--strict-mcp-config`; all 8 cycles from 16:11 to 18:15 ran
  unpatched." The one-number goal was 1. The count is 2 landed, 1 correctly refused.
- **Board: 25 rows, 13 verified, 11 open, 1 blocked.** Nine of today's rows were written to carry work
  out of panes and out of handoff prose.
- **Queue: 13 open cards**, each with steps, a pasteable command and a check. Two were written today
  because an agent was refused (rule 10) and one because an instruction had been living in a closed pane.
- **AGENT-OS rules 11 to 16 written**, carrying Karl's accepted advice with the weight of rule 10.
- **Seven panes restored** by session id in their own workspaces; five stayed closed with their ids
  captured. Seven workspaces restored: tour, lucky-loop, polysignal, brain, platform, loop-factory,
  my-big-game.
- **A second project onboarded**: `~/Projects/my-big-game`, no remote by rule, with its own CLAUDE.md,
  OKRs, brand contract, venture facts, brief and a nine-slide draft deck carrying six visible
  "to confirm" marks. The harness needed no new code to take it.
- **Repos level:** lucky-loop `1bbdaa8` clean, vault clean on both remotes, loop-factory pushed,
  polysignal fast-forwarded 4, platform's council scratch ignored and pushed to both remotes.
- **Two days of compute, measured:** 09-15 $154.50, 09-16 $148.02, 09-17 $31.80 equivalent; 109 M cache
  reads against 0.9 M output tokens on 09-16. The commander session alone was $72.40.

## PRINCIPLES

1. **A list is not a judgement.** The same error twice in one session: thirteen panes closed because
   they were listed, six workspaces destroyed as a side effect. Judge each item, then act.
2. **Blocked is not done. Idle mid-task is not done.** Now rule 16.
3. **Verify against the source, not a stale clone.** A read of the board four commits behind reported a
   task stuck in `claimed` for four hours; the box had verified it and moved on twice. Pull, then read.
4. **Cache reads are the bill, not output.** Eleven agents re-reading the same instruction files every
   turn is what spent two days of limit. Rules 11 to 15 exist for that.
5. **A verifier that is not the builder pays for itself.** The box refused its own third row today on a
   criterion its worker had convinced itself was met.

## OPEN DECISIONS — 13 cards, all in `queue/needs-you.json`

**A word from Karl (3):** `l1-seating-word-2026-09-16` (adopt / keep-seating-human / reject — my
recommendation is keep-seating-human), `dgx-reboot-window-2026-09-16` (a time), and
`amend-08-28-hermes-orchestration-sentence` is its board twin, not a card.

**A hand from Karl (10):** the artifact PR #4 merge; the strict-mcp-config merge; the blocked task
`headless-claude-drop-the-unused-mcp-floor`; the zoetrope binary install (his only because the
classifier refuses agent installs); the Claude settings hook bounds; R-1's last mile; the three
placeholder drafts; the phone app; the ntfy tap test; one new document for the loop (Hop 0 is a
control, by design). The my-big-game card is filed and deliberately not raised here.

## IN FLIGHT

- **The box, detached by design:** nine timers, the listener, the notifier, the artifact-return Action
  every ~6 h, the scanner on its sleeping-beat code. The foreman will find **no eligible row** until
  2026-09-19 05:00Z: the only `dgx` row left is gated behind the plan-limit reset and the other is blocked.
- **The Mac:** 13 herdr agents, all idle except Karl's own working panes. Seven were restored today and
  owe the lines listed in section 12. The 60-second roster snapshot is running.
- **PR #4 is open** and refreshed by the Action twice a day.

**THE ONE MORNING CHECK COMMAND**

```
dgx-today --brief && ~/brain/tools/heartbeat check && ssh dgx-remote 'git -C ~/brain branch --show-current; git -C ~/brain rev-list --count origin/master..HEAD'
```

Expect 13 cards or fewer, `BOX: OK`, an `AGENTS NOW` line, `KR-0 PASS 10/10`, then `master` and `0`.

## DATED PREDICTIONS

| When | Prediction | Proof |
|---|---|---|
| 2026-09-18 00:20–08:00Z | night 3: no `heartbeats -> UNHEALTHY`, `failing=0/14` on every run | `journalctl --user -u infra-watch --since "2026-09-18 00:20" \| grep -c TRANSITION` → 0 |
| 2026-09-18 by 01:45Z | bills-to-loop 0 new, both realm memo jobs blocked on empty realms | that night's nightly capture |
| 2026-09-18 all day | the foreman logs "no eligible open task" on every 15-minute tick until 2026-09-19 05:00Z, unless a human adds a `host: dgx` row | `journalctl --user -u foreman.service \| grep -c "no eligible"` |
| 2026-09-18 by 08:00Z | no `2026-09-18-*` proposals file and `propose.timer` still `not-found`, because the seating word has not been said | `ls ~/brain/queue/proposals/ \| grep -c 2026-09-18` → 0 |

## SHORTLIST

1. The three words and the ten hands above; the seating word unblocks a verifier that is waiting on it.
2. `polysignal-rung1-friction-fields` — the friction win-rate gate metric has read null since 09-05 and
   the fix is one commit on a branch.
3. `transcript-secret-scan-before-any-viewer` — before the zoetrope binary goes in, not after.
4. `foreman-return-vault-clone-to-master` — a worktree per task, so a run never parks the vault clone
   on a branch again. It happened twice more today.
5. `today-desktop-render-proof`, then the rest of the open rows.

## CORPSES

- **Closed thirteen panes because they were on a list**, one of them blocked mid-task with a pending
  action on Karl. Restored by session id; rule 16 written so it cannot recur.
- **Destroyed six workspaces** as a side effect of closing their last tabs. Restored with their labels
  and working directories.
- **Believed a stale clone** and nearly reported a healthy task as stuck for four hours.
- **Two panes could not come back where they were:** a session whose working directory is `$HOME` stops
  at Claude Code's trust prompt and sits blocked forever. They run in project directories now.
- **The `git push origin main` on platform looked like it went nowhere** because `git remote -v \| head -2`
  hid the second remote. Both are level.
- **The my-big-game deck claims no brand-exact type.** Oskari G2 and Rawline are licensed and absent;
  the page says so in its own footer rather than pretending.
