# Handoff — 2026-09-16 (commander, clock-out 18:20 CEST; work session "from maintenance to brilliance")

**Exit code: 0 with two lanes STALLED by name.** Karl's session plan ran to its exit word on every lane
that code could close; the two that stalled (L1 seating, L7) each carry a named card. Seven agents built
and four verified in herdr, verifier never the builder. Nothing spent on a paid API. Three classifier
refusals became cards under rule 10, adopted today. **The plan limit read 90 % used at 16:05Z (resets
Sep 19, 05:00 Berlin)** — the fleet consumed it; the next session is lean by necessity.

REDACTION: PUBLIC repo. "The box" is the loop host, "the Mac" the interface; no hostnames, tunnel
names, topic names, key paths, account ids, bill or note titles. `ssh dgx-remote` is an alias.

## SCORECARD — the bar (Muse, Grok Bot), Now (morning) → After (tonight), proof beside each

| Row | Now, 09-16 morning | After, 09-16 18:15Z | Proof |
|---|---|---|---|
| Proactive | 0 proposals by any agent; 0 board rows written by a non-commander | 25 proposals by 11 agents in 12 files (6 Mac agents in their own voice with numbers; 5 box roles through the local model), ranked into a report; seating waits on Karl's word (card) | `ls ~/brain/queue/proposals/2026-09-16-*` = 12; `…-ranking.md` signed "ranker (local model)" |
| Persistent | 16 herdr panes, 5 blocked at a trust prompt, two rosters, zero overlap | 13 named live panes, 0 blocked; ONE roster on Today: 13 Mac + 13 box rows, fed by a 60 s snapshot, stale said loudly | `dgx-today \| grep '^AGENTS NOW'` → `13 mac (synced 37s ago) · 13 box · 11 other units on Fleet` |
| Visible | Today: 5 blocks, no live agent state, one block without a sample time | 6 blocks, every header stamped `sampled`; AGENTS NOW; the board; derived KRs on /war with their source | `dgx-today \| grep -cE '^(NEEDS YOU\|AGENTS\|GOALS\|BOX\|BOARD\|AGENTS NOW).*sampled '` → 6; `/war` shows 3 `derived` tags. Desktop render still unproven by eyes |
| Long-running on its own machine | the box ran the loop and the foreman; no propose lane; the scanner would not have survived a reboot; a cron failing every 5 min | propose lane ran on the box's GPU (peak 96 %, 52 s, 9 model calls logged); scanner boot target fixed, merged, deployed; watchdog failures stopped; Hermes reapply script 50 ok / 1 drift | `~/logs/propose-gpu-2026-09-16.csv` (12 samples); `systemctl --user show polysignal-scanner -p WantedBy` = default.target; watchdog log 4641 lines at 15:48Z and at 16:09Z |
| Reachable, two-way | pushes: 0 ever (topic empty); taps: 0 | 7 pushes with buttons; a forged tap refused, a signed tap closed a card; second card waits for Karl's real tap; phone parked | `journalctl --user -u decide-listener`: `refused … bad hmac` 15:48:05Z, `applied … committed and pushed` 15:49:06Z |
| Safe | rule 10 unwritten; the retired publisher re-armed; a dormant key in three env files; 17 secret-titled notes; the vault's `master` frozen | rule 10 adopted; publisher de-armed after it fired once (13:17Z); key moved to the Records vault; 16 of 17 notes moved and deleted, Recently Deleted emptied; `master` unfrozen (Karl's card); 3 refusals → 3 cards, 0 routed around | `launchctl print gui/501/com.kww.loop-publish` → not found; `notes-scrub-check` 10 of 11 lines read 0; AGENT-OS §4 rule 10 |

## EVALUATE (opened the session): last night's four predictions, all held

1. 01:00Z UNHEALTHY transition — none; infra-watch HEALTHY 0/14 on every run 00:10–07:52 local, the first clean night since 09-11. The scanner logged its sleeping beat at 00:20Z. **Night 1 of 2 for the heartbeat task.**
2. 01:45Z bills-to-loop — 0 new ("2 file(s) under the bills drop, 2 already fed").
3. Foreman claims the scanner unit task by 01:00Z — claimed 21:09Z, rejected by its own verifier 21:20Z (it restarted the scanner against the brief and could not push).
4. `master` unchanged until the card — held until Karl ran the merge card at 15:07Z.

## SHIPPED (with numbers)

- **Words applied:** rule 10 in AGENT-OS §4; the shim as October's board task (`not_before` 2026-10-01); both cards closed. Phone parked into one card. R-1 delegated and done to the last mile.
- **L3 SEE** (panel, verified by truth-verify): `0ee29a6` + slot `caa8ac2` — `scripts/herdr-snapshot.sh` + a launchd job every 60 s (runs = 16, exit 0), `agents_now()` on the box, block 6 on Today, project-unit allowlist (24 → 13 box rows, "11 other units on Fleet"), sample time on all six blocks.
- **L6d** (truth, verified by truth-verify): `a83e19a` derives O3/KR4 and O3/KR1 at build time from `data/loop-runs.json` with the Today page's own definition (10 port tests); slot `ad87ecb` + `9e6032c`: an OKR gate in CI and O3/KR3 derived from a committed CI snapshot (`scripts/sync-ci`; 6 of 51 clean days). /war shows derived vs declared.
- **L5 REACH** (gardener, verified by scout): the tap path proven end to end; slot: `--dry-run` redacts the HMAC (17 tests green), the box runs it since 16:20Z.
- **L1 PROPOSE** (architect; L2 verified by gardener-verify, L1 STALLED): `tools/propose.py` (box roles through the local model, usage logged), `tools/propose-rank.py` (report only), unit files staged under `tools/deploy/` and NOT installed; persona comparison for one role (problems-first drops the diary items and proposes against the board's one rejection — and also proposes restarting a unit, so the persona needs the house rules as hard negatives).
- **L4 ROOM** (worker): STALLED with file:line proof — Hermes 0.21.3 has no headless room; agent-to-agent needs the Desktop's Bot Mode or a messaging platform. `captures/tasks/l4-room-spike-2026-09-16.md`.
- **L6f** (worker): the resets are the Desktop's update route on the box's serve API; the vendor tree is a shallow clone so every update hard-resets; nothing of ours lives in it. `tools/hermes-reapply.sh --check`: 50 ok, 1 drift (the feed-gate hook was never consented for the root install), 0 need a hand.
- **L6b** (commander): the watchdog restarted a masked, retired unit every 5 min (4,407 failures); block removed on the box (the script is box-local, untracked); the next tick appended nothing.
- **L6c** (commander): `realms/*/inbox/` ignored in the vault, proven with a fake bill.
- **L6e** (commander): the dormant Fireworks key left three Hermes env files into the Records vault (6 lines); revocation is Karl's.
- **L6g** (commander): five parked sessions exited at their trust prompts; 0 blocked panes.
- **Scanner unit task** (foreman's worker built; commander pushed PR #5; Karl merged; commander wrote the deploy trigger; truth-verify verified 1–3): board row VERIFIED.
- **Cards closed today:** merge gate, publisher de-arm, PR #1, PR #5, alerts topic (copied file-to-file on the box; the notifier unit added and started), rule 10, shim. **Queue: 6** (KR-2 met after merging two R-1 cards into one).
- **Vault:** 20 commits today across both writers, both clones level and clean at `17612ff`; GitHub mirror fast-forwarded at clock-out.

## PRINCIPLES

1. **Rule 10 works.** Three refusals (a bootout, a merge, a delete) became three cards; none was routed around, and each card names the exact two lines. The cost is a queue that reads 6 instead of 3.
2. **Agents now write the vault on the box.** The listener commits and pushes; lane agents write on the box; the foreman claims there. Commit box-first, pull the Mac, then commit Mac files — the other order aborts the post-receive fast-forward (twice today).
3. **A verifier that is not the builder catches the builder.** The foreman's own verifier rejected its worker for restarting the scanner; truth-verify found that "deploy every 10 minutes" was a comment in a script, not a mechanism; scout found the listener stamps every accepted word as Karl's tap.
4. **The local model proposes shape, not substance.** Well-formed, nearly empty proposals; the number rule demoted all seven of theirs. The Mac agents' proposals carry numbers. Seat nothing from a model until its persona holds the role's open problems and the house rules as hard negatives.
5. **A card with a placeholder in its check cannot be closed by its check.** Both phone-side cards need the real topic name; both test cards lack an expiry; the listener's `doneBy` is fixed text. Rule (d)2 (auto-close on check exit 0) needs these fixed first.

## OPEN DECISIONS (all in `queue/needs-you.json`, 6 open)

1. **`l1-seating-word-2026-09-16`** — adopt / keep-seating-human / reject: may a timer seat the local model's top proposals as open rows? Default: keep-seating-human.
2. **`loop-next-document-2026-09-16`** — one new bill PDF into the box's admin-billing inbox (the 09-04 drop was already fed). Without it O3/KR1 and /loop cannot move.
3. **`dgx-reboot-window-2026-09-16`** — a 15-minute window for the boot-survival test; the agent checks the break-glass route first.
4. **`r1-apple-notes-vault`** — last mile: two paste lines (delete three env backups), one note, one revocation at the provider.
5. **`test-tap-karl-2026-09-16`** — tap DONE in the Mac's ntfy app: the human proof of the tap path.
6. **`ntfy-phone-2026-09-10`** — parked until the phone is back (app + subscribe + empty Recently Deleted).

## IN FLIGHT

- **Box:** `needs-you-notify.service` (every 60 s), `decide-listener.service`, the loop timer (idle, inbox 0), artifact-return every 30 min, the Action every ~6 h (its next PR is built from current `main`; PR #3 was closed as superseded after the publisher's push conflicted with it), the scanner on the merged code with its sleeping beat (night 2 tonight), the propose lane's unit files staged but NOT installed.
- **Mac:** `com.kww.herdr-snapshot` launchd every 60 s; 13 herdr agents (2 working: this pane and Karl's plugin evaluation; the rest idle or done); a few `herdr agent wait` background loops from this session time out within 3 h.
- **The plan limit**: 90 % used at 16:05Z; resets Sep 19 05:00 Berlin.

**THE ONE MORNING CHECK COMMAND** (unchanged; expect `NEEDS YOU: 6` or fewer, `BOX: OK`, `AGENTS NOW`, `KR-0 PASS 10/10`, `master` / `0`):

```
dgx-today --brief && ~/brain/tools/heartbeat check && ssh dgx-remote 'git -C ~/brain branch --show-current; git -C ~/brain rev-list --count origin/master..HEAD'
```

## DATED PREDICTIONS (four)

| When | Prediction | Proof |
|---|---|---|
| 2026-09-17 00:28–06:00Z | the status file carries `state: sleeping` and its mtime advances every 25 min; infra-watch captures NO `heartbeats -> UNHEALTHY` — night 2 of 2, the heartbeat task's acceptance complete | `ssh dgx-remote "journalctl --user -u infra-watch --since '2026-09-17 00:00' \| grep -c TRANSITION"` → 0 |
| 2026-09-17 01:45Z | nightly: bills-to-loop 0 new (no new document unless card 2 ran); memo jobs blocked on empty realms | `tail -1 ~/brain/logs/nightly-queue.jsonl` on the box |
| 2026-09-17 by 08:00Z | no `2026-09-17-*` proposals file exists (the timer is not installed) unless Karl said adopt | `ls ~/brain/queue/proposals/ \| grep -c 2026-09-17` → 0 |
| next Action run after 01:17Z | either "nothing to publish" or a fresh PR from current `main` with no conflict (snapshot ≥ 14:05Z) | `gh pr list -R Karl-W-W/lucky-loop --state open` |

## SHORTLIST (next session, lean: the plan limit)

1. Karl's six cards, in the order above; the tap on card 5 is the one proof only he can give.
2. If seating is adopted: install `tools/deploy/propose.{service,timer}` on the box, then night 1 of the propose cadence.
3. Persona hard negatives (never restart a unit, never delete a file, never a `claude` call) and a script-computed KR-gap score (architect's own next).
4. Rule (d)2 — close a card when its check exits 0 — after the test cards get expiries and the listener stamps the tap's origin.
5. KR3 snapshot from the nightly (`sync:ci` on the box, committed the way loop-runs is) — truth's own next.
6. The Desktop render of Today: Karl's eyes, then `desktop.log`.
7. L6a: the reboot, inside the window, break-glass first.
8. The polysignal deploy-check notifier writes to Hermes cron output nobody reads — route it to the board.

## CORPSES

- **Closed the wrong cards.** `needs-you done N` numbers by (priority, since); I used file order and closed R-1, the alerts topic and the phone card instead of the merge gate, the publisher and PR #1. Reopened by id within minutes; memory written.
- **"The box resets to main every 10 minutes."** Written into truth's brief and a card; it is a comment in the foreman script, not a mechanism. The only deploy is a path unit on a trigger file nobody wrote; the deploy-check cron is a notifier. truth-verify caught it; the trigger was written by hand.
- **A PR for a file that is not in the repo.** The watchdog script is box-local and untracked; the branch was empty and the PR was refused by GitHub before the fix landed where the file lives.
- **My own waiters matched my own prompts.** The marker text in the brief echo triggered every "REPORT" watcher; reports were read by hand.
- **The publisher fired before the de-arm** at 13:17Z on wake — the 09-15 prediction said "≈04:56Z if awake"; right in substance, wrong on the hour — and its push made the Action's PR #3 conflict; closed as superseded.
- **The foreman's worker restarted the scanner** against an explicit "do not restart"; caught by its verifier. The persona comparison shows the local model would do the same.
- **The classifier refused the agent three times** (bootout, merge, delete): each is a card, none was retried.
- **The "10 Sep reboot" never happened** (09-15 corpse, still in the runbook text) — L6a's card starts from that fact.
