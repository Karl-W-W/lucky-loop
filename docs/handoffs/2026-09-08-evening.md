# Handoff — 2026-09-08 (evening: verifications, R5 tiers, closes, /war numbers)

Written for the next session. **Every claim here is a hypothesis** — each was checked by the
commander against git, the box, GitHub's API or the live page, and the check is named.

REDACTION: PUBLIC repo. No hostnames, tunnel names, fingerprints, tokens, auth URLs.

Session shape: Karl took the afternoon report "as a lead, not a finding" and ordered
verifications with checks, an R5 amendment (three tiers), closes, and one feature: three numbers
on /war. All done; one foreman defect found and fixed on the way.

## SHIPPED (with numbers)

- **Verified**: the box's factory checkout is on the read-only deploy key (GitHub API
  `read_only=true`), push URL disabled. The two doc pushes to this repo went through the server
  gate (`gates.yml` success on both SHAs); the pre-commit hook ran and had nothing to check
  (docs only). Heartbeats now print UTC and the check runs inside infra-watch every 15 min
  (`heartbeats` row in the infra dashboard: 7/7). The foreman's first worker raised two items by
  itself; neither was caused by a denied command.
- **R5 amended** (protocol, agent template, queue README): tier 1 default at next tick, tier 2
  next Sunday unless vetoed (`queue/proposals.json`), tier 3 ask; expiry ≤ 7 days; `closes_by`.
- **Queue**: 10 items classified; 4 closed (mirror realigned, allow-pins applied, first-live-run
  by default, hermes-platform decided later). **NEEDS YOU: 6**, under 7 before the box's lap.
- **Mirror realigned without loss**: the three commits only the mirror had were content-identical
  (patch-id) to local commits, so a push with lease was allowed by Karl's rule. Both remotes 0/0.
- **Denials → proposals**: 13 refused tool calls today on the box; 6 were workers inspecting
  secret paths because the acceptance asked for it (a brief defect, filed as a tier-1 fix), 4 became
  tier-2 allow proposals for Sunday, 3 were the commander's own probes.
- **/war shows three numbers** (needs-you, heartbeats fresh, laps converged unattended) from a
  committed snapshot; every gate green (redaction, drift, roster, mcp, live), server gate success,
  production deploy live. Editing the dashboard trips the drift gate; the canvas was regenerated.
- **Foreman defect fixed**: a whole-board save at the end of a cycle reverted the commander's
  re-open of the loop row. Now one row is written into a freshly pulled board.

## PRINCIPLES

1. **A report is a lead until each claim carries its check.** Show the check, not the sentence.
2. **One row, fresh board.** A long-running process must never write back an object it loaded an
   hour ago.
3. **Deny rules are not defects; briefs that ask for the forbidden are.** Six denials were the
   acceptance text asking to stat secret paths.
4. **Force only on proven identity.** Patch-id twins, stated explicitly, or no force.
5. **Tier 2 lives outside NEEDS YOU**, so the count stays a KR about human hands.

## OPEN DECISIONS (tier 3, in the queue with default + expiry ≤ 7 days)

1. Dead-man: Karl's five steps tonight (`deadman-checklist-2026-09-08`).
2. Feed-from-bills branch → public repo (`feed-from-bills-push-2026-09-08`).
3. Lever merge, gate install, loop-publish install, R-1 notes vault (unchanged).

## IN FLIGHT

- The box's first mail-triage lap: the row is `open`, the foreman's dry run names it next; the
  15-min tick claims it. Expect CONVERGED (three KRs pass now).
- Tier 2 proposals apply Sunday 2026-09-13 unless vetoed in `queue/proposals.json`.
- Nightly queue 03:30; the feed-from-bills job runs live for the first time (tier 1, let-it-run).

**THE ONE MORNING CHECK COMMAND**

```
dgx-today --brief && ~/brain/tools/heartbeat check
```

Expect `NEEDS YOU: 6 item(s)` or fewer, a `foreman` row for the loop with `CONVERGED`, `BOX: OK`,
`KR-0 PASS 7/7`. Then `npm run sync:ops` here and commit, so /war's numbers move.

## DATED PREDICTIONS

- **2026-09-08 by 18:30**: the loop row reads `verified`, `lastExit CONVERGED`, lap 1, and the
  capture exists. Falsified if the row is `blocked` or still `open`.
- **2026-09-09 03:40**: KR-0 still 7/7 after the nightly run, and the nightly page carries a
  `bills-to-loop` section. Falsified if either is missing.
- **2026-09-13**: the four tier-2 proposals are applied (or vetoed) and `permission-denials.jsonl`
  shows fewer than 13 rows for that day.

## SHORTLIST

1. Karl's five dead-man steps.
2. Apply the brief fix: acceptance texts must not ask for secret-path mtimes.
3. The Hermes loop-session wire (card 013), once the mechanism is verified.
4. `npm run sync:ops` each clock-out so /war's three numbers stay honest.

## CORPSES

- "The row is open" — the foreman's stale board wrote `paused` back over it. Fixed at the cause.
- "The PermissionRequest hook logs denials on the box" — it logged nothing; the transcript does.
- "`uname -a` running under Read-only means bypass mode" — read-only commands are auto-approved.
- "Write(…) deny rules protect the verifier" — inert; Edit rules do the work.
- "NEEDS YOU 9 or fewer" (afternoon prediction) — it was 10: the foreman's worker added two.
