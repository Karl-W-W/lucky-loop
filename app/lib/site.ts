/* One place for the canonical origin. It moves to the custom domain in the
 * same change that makes that domain answer 200 — never before, because
 * opengraph-image URLs are absolute and a dead origin means dead share cards. */
export const SITE_URL = "https://lucky-loop-one.vercel.app";
